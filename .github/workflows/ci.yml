name: ci

on:
  push:
    branches: [main, ci-matrix-probe]
  pull_request:

jobs:
  build:
    name: ${{ matrix.os }} · node-${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # npm/cli#4828 (Linux-generated lockfile missing per-platform
        # optional deps for Windows/macOS runners) was closed 2025-04-03;
        # lockfile regenerated 2026-04-19 now carries 25 per-platform
        # rollup entries, so the three-OS matrix can come back.
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - name: Install deps
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Build
        run: npm run build
      - name: Unit tests
        run: npm test
      - name: Smoke-check built bins
        shell: bash
        run: |
          node dist/cli.js version
          # MCP bin requires config; expect clean exit (code 3) on missing config
          # rather than crash. stdin is closed in CI, so the server won't hang.
          node dist/mcp.js --scope global < /dev/null || test $? -eq 3
