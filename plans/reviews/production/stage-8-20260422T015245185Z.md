---
type: review-report
review_type: production
stage: 8
verdict: PASS
run_id: production-8-20260422T015237916Z
created_at: 2026-04-22T01:52:45.185Z
---

# production — Stage 8 — PASS

## Summary

The diff introduces project adoption functionality and config defaults for LLM tools, with no new data stores or critical durability surfaces. The project remains a CLI tool, so service-grade DR requirements don't apply. Backup considerations for user data files are noted but not required for a developer tool. All findings are either category errors or low-severity.

## Findings

- **info** — `src/cli.ts:790` — Added 'vcf adopt' command to adopt existing project directories into VCF tracking without scaffolding docs/plans. This is a developer CLI tool functionality, not a service-grade artifact.
  - required: No change required - this is correct for a CLI tool
- **info** — `src/config/schema.ts:210` — Added 'defaults' block to config schema allowing per-step model/endpoint defaults. This is a developer tool enhancement, not a service-grade artifact.
  - required: No change required - this is correct for a CLI tool
- **info** — `src/db/schema.ts:199` — Added project_adoption_flag migration v2 to track projects brought into VCF via project_init_existing. This is for developer tool provenance tracking, not service-grade durability.
  - required: No change required - this is correct for a CLI tool
- **info** — `src/tools/review_execute.ts:45` — Made endpoint field optional in review_execute tool, falling back to config.defaults.review.endpoint. This is a developer tool enhancement, not a service-grade artifact.
  - required: No change required - this is correct for a CLI tool
- **info** — `src/tools/ship_release.ts:246` — Added project state transition to 'shipped' on successful release. This is a developer tool state tracking, not a service-grade artifact.
  - required: No change required - this is correct for a CLI tool
- **info** — `src/util/templates.ts:36` — Fixed templatesDir() to resolve package root correctly in both dev and bundled layouts. This is a developer tool fix, not a service-grade artifact.
  - required: No change required - this is correct for a CLI tool

## Carry-forward

### architecture

_none_

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 8, info) User data files (.vcf/project.db and ~/.vcf/vcf.db) are SQLite files that can be backed up with cp. This is noted in README under 'Data & Backup' section.

### release_confidence

- (stage 8, info) The project remains a developer CLI tool with no service-grade durability requirements. All changes are within the scope of CLI tool enhancements.

