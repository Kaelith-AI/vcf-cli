---
type: review-report
review_type: production
stage: 9
verdict: PASS
run_id: production-9-20260422T005449282Z
created_at: 2026-04-22T00:55:34.641Z
---

# production — Stage 9 — PASS

## Summary

The diff introduces critical operational fixes for template resolution in bundled installs, implements a governance-friendly 'adopt' flow for existing projects, and adds configuration defaults to reduce per-call overhead. All changes are backed by integration tests and schema validation. The fix for `templatesDir()` is particularly high-value as it resolves a production-blocking ENOENT error for all users of versions 0.3.0-0.3.2. The state transition to 'shipped' provides the necessary observability for portfolio-level governance.

## Findings

- **info** — `src/util/templates.ts:34` — The `findPackageRoot` function includes a fallback to `resolvePath(start, '..', '..')`. While this preserves legacy behavior, it may be fragile if the directory structure changes further. However, given the current bundle layout, this is an acceptable operational trade-off.
  - required: None; documented as a fallback.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 9, info) The 'adopt' flow allows projects to enter VCF tracking without full scaffolding. Operators should be aware that these projects have 'partial provenance' (marked by the `adopted` flag), which may affect future strict-mode audits.

### release_confidence

- (stage 9, info) High confidence. The critical template resolution bug was surfaced by a stress harness and is now verified via the bundled layout logic. Schema migrations for the `adopted` flag are automatic and non-destructive.

