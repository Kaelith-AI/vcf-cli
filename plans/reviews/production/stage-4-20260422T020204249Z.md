---
type: review-report
review_type: production
stage: 4
verdict: PASS
run_id: production-4-20260422T020155034Z
created_at: 2026-04-22T02:02:04.249Z
---

# production — Stage 4 — PASS

## Summary

The diff introduces project adoption capabilities and config-driven defaults for LLM tools, with no new production-significant surfaces. Artifact class is confirmed as developer CLI tool, not a service, so production stage 4 capacity/performance checks apply at tool-grade, not service-grade. All resource bounds and overload behaviors are appropriate for a CLI tool's operational scope.

## Findings

- **info** — `src/cli.ts:790` — Added runAdopt function with idempotent re-adoption logic and registry write that's non-fatal on failure, matching MCP tool's pattern
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/cli.ts:808` — Config loading and allowed_roots enforcement added to CLI adopt command, matching MCP tool's safety guarantees
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/tools/review_execute.ts:45` — Endpoint field in review_execute input schema now optional with description pointing to config defaults
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/tools/review_execute.ts:100` — Endpoint resolution now follows explicit arg → config.defaults.review.endpoint → E_VALIDATION order
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/tools/review_execute.ts:154` — Provider-specific options (providerOptions) now passed through to LLM calls when defined on endpoint
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/tools/ship_release.ts:274` — Project state transition to 'shipped' now gated on successful gh exit (0), preserving state on failure
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/util/templates.ts:36` — Fixed templatesDir() path resolution to work in both dev and bundled layouts using walk-up to package.json
  - required: No change required - behavior is correct for CLI tool scope
- **info** — `src/db/schema.ts:201` — Project DB migration v2 adds adopted column with default 0, documented rollback path in CHANGELOG
  - required: No change required - behavior is correct for CLI tool scope

## Carry-forward

### architecture

- (stage 2, info) Deploy-shape truth: still a developer CLI/MCP tool with local-process execution. New adoption flow persists authoritative state in `<project>/.vcf/project.db`; `~/.vcf/vcf.db` remains advisory cache/registry only.

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 1, info) Artifact class confirmed as developer CLI/MCP tool, not a production service. Downstream production stages should apply tool-grade criteria (README/help/install/error clarity/destructive-op safeguards), not service-grade pager/SLO/runbook requirements.
- (stage 1, info) Service identity and intent are explicit in README and CLI help: existing-project adoption via `vcf adopt`, plus config-driven review routing. Failure contract is bounded for adoption: allowed-roots enforced when config exists, malformed config fails closed, registry write is advisory and recoverable by rerun.
- (stage 2, info) State durability risk is bounded and explicit for tool scope: `runAdopt` writes local project DB first, then attempts global registry update non-fatally. Re-running `vcf adopt` heals registry drift; README/CHANGELOG now document the storage locations and rollback expectations.
- (stage 3, info) Tool-grade observability posture is acceptable for this diff. `review_execute` now preserves more truthful review outcomes on Ollama via endpoint-scoped `provider_options`, reducing silent prompt truncation risk on long review prompts.
- (stage 3, info) `ship_release` now keeps redacted stdout/stderr tails in the success envelope and audit content, improving operator diagnosis of failed GitHub release attempts without exposing raw secrets.
- (stage 4, info) Artifact class confirmed as developer CLI/MCP tool, not a production service. Downstream production stages should apply tool-grade criteria (README/help/install/error clarity/destructive-op safeguards), not service-grade pager/SLO/runbook requirements.

### release_confidence

- (stage 1, info) Schema migration v2 is additive (`project.adopted`) and the CHANGELOG now documents rollback expectations for tool users: manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup after package downgrade.
- (stage 2, info) No new single-instance illusion or false horizontal-scaling claim introduced. Config defaults and endpoint provider_options affect request routing only; they do not create durable shared state or contradict the local-process deployment model.
- (stage 3, info) No production-stage-3 blocking telemetry theater found in the scoped diff. No service health/readiness surface was added, and no misleading monitoring/dashboard claims were introduced.
- (stage 4, info) Schema migration v2 is additive (project.adopted column) and the CHANGELOG now documents rollback expectations for tool users: manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup after package downgrade.
- (stage 4, info) No new single-instance illusion or false horizontal-scaling claim introduced. Config defaults and endpoint_provider_options affect request routing only; they do not create durable shared state or contradict the local-process deployment model.

