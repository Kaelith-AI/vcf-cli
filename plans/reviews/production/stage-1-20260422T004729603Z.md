---
type: review-report
review_type: production
stage: 1
verdict: BLOCK
run_id: production-1-20260422T004712831Z
created_at: 2026-04-22T00:47:29.603Z
---

# production — Stage 1 — BLOCK

## Summary

Stage 1 fails on ownership/accountability and operational intent. The diff introduces a new adopt surface (`vcf adopt` CLI and `project_init_existing` MCP registration), new state transitions, and config-default routing behavior, but the scoped evidence contains no named owner, no pager/escalation path, no README/runbook/service doc updates defining the service’s operational contract, and no explicit degraded/failure contract for the newly added adoption path or migration behavior. Later production stages would be guesswork because this change expands service scope without corresponding operational definition artifacts.

## Findings

- **blocker** — `src/cli.ts:1652` — New externally used service surface `vcf adopt` is added, but the scoped diff provides no ownership artifact, support contact, or escalation path for operating/fixing adoption failures. Stage 1 requires an identifiable owner and accountable routing; none is evidenced in README/service docs/CODEOWNERS/runbook updates in this change.
  - required: Add or update service/operations documentation and ownership metadata so the adopt workflow has a named owner/team and pager-reachable escalation/contact path, consistent across repo artifacts.
- **blocker** — `src/server.ts:158` — The MCP server now registers `project_init_existing`, expanding the service contract for project bootstrap/adoption, but no service-definition documentation in the diff explains who this capability is for, whether it is internal or external operator-facing, or what support scope applies when adoption fails or partially succeeds.
  - required: Document the service identity and audience for the new adoption capability, including primary entry points, intended operators, and support boundaries.
- **blocker** — `src/db/schema.ts:199` — Project DB migration v2 adds persistent adoption state (`adopted`), but the diff includes no operational note/runbook describing migration ownership, expected rollout behavior, or how operators detect and handle migration failure on next open. This leaves success/failure of a stateful path undefined.
  - required: Add migration operational guidance covering owner, when migration runs, expected failure symptoms, and operator response/recovery steps.
- **blocker** — `src/cli.ts:787` — `runAdopt` creates `.vcf/project.db`, mutates project metadata, and writes to the global registry, but the acceptable failure contract is not documented. The code can partially succeed across per-project DB and global registry updates, yet the diff does not define whether partial adoption is acceptable, how to recognize it, or what degraded-but-acceptable state means.
  - required: Document the failure contract for adoption, including partial-write scenarios, expected operator-visible outcomes, and recovery/cleanup procedure.
- **warning** — `src/tools/ship_release.ts:240` — Release success now mutates `project.state` to `shipped` and best-effort mirrors global registry state, but the scoped diff does not update service docs or operational notes to describe this as part of the service’s supported lifecycle contract. The code comments state the registry mirror is non-fatal, leaving cross-store divergence implicitly tolerated without an explicit operational statement.
  - required: Record the lifecycle-state contract and whether registry/cache divergence is acceptable, including who owns remediation if mirror update fails.
- **warning** — `src/tools/review_execute.ts:97` — Review execution now depends on `config.defaults.review.endpoint` when explicit input is omitted, changing operator expectations for routing and failure (`E_VALIDATION` if unset). The service’s operational intent for review routing, default selection, and misconfiguration handling is not documented in service/operator-facing docs in the scoped diff.
  - required: Document the review-routing intent, configuration precedence, and expected operator action when defaults are absent or invalid.

## Carry-forward

### architecture

- (stage 1, warning) State now spans per-project DB plus global registry/cache with best-effort mirroring. Production 2 should treat cross-store consistency and authoritative-source assumptions as explicit review targets.

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 1, blocker) Service identity is incomplete for the newly added adoption/bootstrap surface. No scoped evidence names an operational owner, escalation path, or support boundary for `vcf adopt` / `project_init_existing`.
- (stage 1, blocker) Failure-contract uncertainty remains for adoption and migration paths: partial success across project DB and global registry is possible, but acceptable degraded state and operator recovery are not defined.

### release_confidence

- (stage 1, warning) Maturity is overclaimed relative to operational evidence: new bootstrap, migration, and routing behaviors landed without matching README/runbook/ownership documentation, so release readiness cannot yet be judged as production-defined.

