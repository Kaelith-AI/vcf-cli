---
type: review-report
review_type: production
stage: 3
verdict: PASS
run_id: production-3-20260422T020140682Z
created_at: 2026-04-22T02:01:47.988Z
---

# production — Stage 3 — PASS

## Summary

The diff introduces project adoption functionality and config defaults for LLM tools, with proper validation and error handling. No production-significant telemetry or operational evidence gaps found for a CLI tool. The artifact class is correctly identified as developer CLI, not a production service.

## Findings

- **info** — `src/cli.ts:790` — Added runAdopt function with proper validation and error handling for project adoption, including allowed_roots enforcement and non-fatal registry write with warning
  - required: None
- **info** — `src/config/schema.ts:210` — Added DefaultsSchema with validation for tool-specific endpoint/model defaults, including cross-reference validation against declared endpoints
  - required: None
- **info** — `src/tools/review_execute.ts:45` — Updated review_execute tool to support optional endpoint parameter with fallback to config.defaults.review.endpoint
  - required: None
- **info** — `src/tools/review_execute.ts:154` — Added provider_options support for endpoint-specific LLM parameters, preventing silent truncation in Ollama endpoints
  - required: None
- **info** — `src/tools/ship_release.ts:246` — Enhanced ship_release to transition project.state to 'shipped' on successful release and properly redact stdout/stderr
  - required: None
- **info** — `src/util/templates.ts:36` — Fixed templatesDir() to resolve correctly in both dev and bundled layouts using package root walk-up
  - required: None

## Carry-forward

### architecture

- (stage 2, info) Deploy-shape truth: still a developer CLI/MCP tool with local-process execution. New adoption flow persists authoritative state in `<project>/.vcf/project.db`; `~/.vcf/vcf.db` remains advisory cache/registry only.

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 1, info) Artifact class confirmed as developer CLI/MCP tool, not a production service. Downstream production stages should apply tool-grade criteria (README/help/install/error clarity/destructive-op safeguards), not service-grade pager/SLO/runbook requirements.
- (stage 1, info) Service identity and intent are explicit in README and CLI help: existing-project adoption via `vcf adopt`, plus config-driven review routing. Failure contract is bounded for adoption: allowed-roots enforced when config exists, malformed config fails closed, registry write is advisory and recoverable by rerun.
- (stage 2, info) State durability risk is bounded and explicit for tool scope: `runAdopt` writes local project DB first, then attempts global registry update non-fatally. Re-running `vcf adopt` heals registry drift; README/CHANGELOG now document the storage locations and rollback expectations.
- (stage 3, info) Artifact class confirmed as developer CLI/MCP tool, not a production service. Downstream production stages should apply tool-grade criteria (README/help/install/error clarity/destructive-op safeguards), not service-grade pager/SLO/runbook requirements.

### release_confidence

- (stage 1, info) Schema migration v2 is additive (`project.adopted`) and the CHANGELOG now documents rollback expectations for tool users: manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup after package downgrade.
- (stage 2, info) No new single-instance illusion or false horizontal-scaling claim introduced. Config defaults and endpoint provider_options affect request routing only; they do not create durable shared state or contradict the local-process deployment model.
- (stage 3, info) Schema migration v2 is additive (project.adopted column) and the CHANGELOG documents rollback expectations for tool users: manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup after package downgrade.

