---
type: review-report
review_type: production
stage: 9
verdict: PASS
run_id: production-9-20260422T015250482Z
created_at: 2026-04-22T01:53:03.004Z
---

# production — Stage 9 — PASS

## Summary

The vcf-cli 0.4 release introduces developer-facing features including project adoption, per-step defaults, and improved template resolution. The diff is scoped to a CLI tool with no new production services, so the production bar applies only to the developer-facing surface. All findings from earlier stages have been addressed or appropriately categorized as non-service artifacts. The release is ready for sustained operation within its intended scope.

## Findings

- **info** — `CHANGELOG.md:15` — Added project_init_existing tool and vcf adopt CLI for adopting pre-existing projects into VCF tracking. This is a developer CLI feature, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `CHANGELOG.md:28` — Per-step model/endpoint defaults added via config.defaults block. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `CHANGELOG.md:41` — Fixed templatesDir() to resolve to the correct directory in published installs. This is a developer CLI fix, not a service-grade artifact.
  - required: No change required - this is a developer tool fix as intended.
- **info** — `CHANGELOG.md:51` — ship_release now transitions project.state to 'shipped' on successful release. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `CHANGELOG.md:63` — Project DB migration v2 adds 'adopted' column. Rollback documented for user-level recovery. This is a developer CLI schema change, not a service-grade artifact.
  - required: No change required - rollback path documented appropriately.
- **info** — `CHANGELOG.md:75` — Reviewer overlays bumped to v0.2 with explicit verdict-calibration rules and evidence-discipline rules. This is a reviewer system update, not a service-grade artifact.
  - required: No change required - this is a reviewer system update as intended.
- **info** — `src/cli.ts:790` — Added runAdopt function to adopt existing projects into VCF tracking. This is a developer CLI feature, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/cli.ts:1776` — Added 'adopt' command to CLI. This is a developer CLI feature, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/config/schema.ts:83` — Added provider_options to EndpointSchema for Ollama-specific options. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/config/schema.ts:210` — Added DefaultsSchema for per-step defaults. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/db/schema.ts:197` — Added project_adoption_flag migration. This is a developer CLI schema change, not a service-grade artifact.
  - required: No change required - rollback path documented appropriately.
- **info** — `src/server.ts:28` — Registered project_init_existing in server. This is a developer CLI feature, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/tools/review_execute.ts:45` — Made endpoint optional in ReviewExecuteInput and added fallback to config.defaults.review.endpoint. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/tools/ship_release.ts:246` — Added project state transition to 'shipped' on successful release. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/util/llmClient.ts:41` — Added providerOptions to ChatCompletionRequest for Ollama-specific options. This is a developer CLI enhancement, not a service-grade artifact.
  - required: No change required - this is a developer tool feature as intended.
- **info** — `src/util/templates.ts:30` — Fixed templatesDir() to resolve correctly in both dev and bundled layouts. This is a developer CLI fix, not a service-grade artifact.
  - required: No change required - this is a developer tool fix as intended.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 8, info) Stage 8 scoped surface is limited to developer-local SQLite state (`<project>/.vcf/project.db`, `~/.vcf/vcf.db`). Changelog now documents practical backout/restore handling for the additive v2 schema change; no service-grade backup/DR gate applies to this CLI artifact.
- (stage 9, info) Stage 8 scoped surface is limited to developer-local SQLite state (`<project>/.vcf/project.db`, `~/.vcf/vcf.db`). Changelog now documents practical backout/restore handling for the additive v2 schema change; no service-grade backup/DR gate applies to this CLI artifact.

### release_confidence

- (stage 8, info) No new critical data store or restore-theater risk introduced by this diff. Durability impact is bounded to additive local-state schema evolution and remains acceptable for release as a developer tool.
- (stage 9, info) No new critical data store or restore-theater risk introduced by this diff. Durability impact is bounded to additive local-state schema evolution and remains acceptable for release as a developer tool.

