---
type: review-report
review_type: production
stage: 7
verdict: PASS
run_id: production-7-20260422T015205483Z
created_at: 2026-04-22T01:52:22.140Z
---

# production — Stage 7 — PASS

## Summary

The diff introduces `vcf adopt` and per-step defaults for LLM tools, with improved error handling, config validation, and audit logging. Artifact class is correctly identified as developer CLI/tool, not service-grade, so production stage 7 gates apply appropriately. No service-grade operational artifacts required for CLI components.

## Findings

- **info** — `src/cli.ts:790` — New `runAdopt` function added to handle adoption of existing project directories into VCF tracking. Includes safety checks for allowed roots and graceful handling of global registry failures.
  - required: None - functionality is correctly implemented and tested.
- **info** — `src/cli.ts:1776` — New CLI command `vcf adopt` added with options for path, name, and initial state. Command properly integrates with existing project and registry handling.
  - required: None - command is correctly implemented.
- **info** — `src/config/schema.ts:210` — Added `DefaultsSchema` to support per-step model/endpoint defaults in config.yaml. Includes validation to ensure referenced endpoints exist.
  - required: None - schema is correctly implemented with validation.
- **info** — `src/db/schema.ts:201` — Project DB migration v2 added to include `adopted` column. Migration is additive and auto-migrates existing databases.
  - required: None - migration is correctly implemented.
- **info** — `src/server.ts:28` — New MCP tool `project_init_existing` registered in server. This aligns with the CLI adoption functionality.
  - required: None - tool registration is correctly implemented.
- **info** — `src/tools/review_execute.ts:45` — Endpoint field in `review_execute` tool now optional, with fallback to config defaults. This enables per-step default configuration.
  - required: None - tool correctly implements default resolution.
- **info** — `src/tools/ship_release.ts:246` — Ship release now transitions project state to 'shipped' on successful completion. Also preserves redacted stdout/stderr in audit bundle.
  - required: None - functionality correctly implemented with proper error handling.
- **info** — `src/util/llmClient.ts:42` — Added `providerOptions` to `ChatCompletionRequest` interface to support endpoint-specific options like Ollama's `num_ctx`.
  - required: None - feature correctly implemented with documentation.
- **info** — `src/util/templates.ts:36` — Fixed `templatesDir()` resolution to work correctly in both development and bundled installations by walking up to find package root.
  - required: None - fix correctly implemented.
- **info** — `test/config/schema.test.ts:156` — Added tests for defaults block validation, including endpoint reference validation and strict schema enforcement.
  - required: None - tests correctly cover new functionality.
- **info** — `test/integration/review_execute.test.ts:333` — Added integration tests for default endpoint/model resolution, explicit arg override, and provider_options passing.
  - required: None - tests correctly cover new functionality.
- **info** — `test/integration/ship_release.test.ts:144` — Added test to verify that failed releases do not transition project state from 'shipping' to 'shipped'.
  - required: None - test correctly verifies state transition behavior.

## Carry-forward

### architecture

- (stage 2, info) Deploy-shape truth: artifact remains a local developer CLI plus MCP server process, not a standalone production service. New `vcf adopt` / `project_init_existing` flows continue to use per-project SQLite in `<project>/.vcf/project.db` as authoritative state, with `~/.vcf/vcf.db` as advisory registry cache.

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 1, info) Artifact-class gate applied: this repo is primarily a developer CLI/tool with an MCP server component, so Stage 1 was evaluated on tool operational intent and maintainership clarity rather than service-grade pager/SLO criteria.
- (stage 1, info) Operational identity in diff is clear enough for later stages: `vcf adopt`/`project_init_existing` adopt pre-existing directories into VCF tracking; local `.vcf/project.db` remains authoritative when global registry sync fails; failed `ship_release` does not transition state.
- (stage 2, info) State durability risk remains bounded to user-operated SQLite files. Migration v2 (`project.adopted`) is additive and does not change the core ownership boundary: local project DB is authoritative; global registry write failures are explicitly tolerated and recoverable by re-running adopt.
- (stage 3, info) Stage 3 artifact-class calibration held: no new service-grade observability obligation was introduced by this CLI/MCP diff. Operational evidence expectations remain CLI-centric (legible errors, actionable command output, truthful local state transitions).
- (stage 3, info) `ship_release` now preserves redacted stdout/stderr tails in the success envelope and audit bundle (`src/tools/ship_release.ts`), improving post-failure diagnosis versus the prior `<redacted>` placeholder that removed nearly all operator signal.
- (stage 4, info) Stage 4 artifact-class calibration held: reviewed as developer CLI/tool plus MCP server component, not as a fleet service. No new service-grade capacity artifact is required from this diff.
- (stage 5, info) Stage 5 artifact-class calibration held: release/recovery posture was reviewed as developer CLI/tool plus MCP server component, not as a fleet service. No service-grade rollout/runbook/SLO findings apply to the CLI-only surfaces added here.
- (stage 6, info) Stage 6 artifact-class calibration held: scoped changes remain primarily developer CLI/MCP flows, so resilience review focused on bounded command/tool failure behavior rather than service-grade circuit-breaker/SLO/on-call expectations.
- (stage 6, info) Dependency-failure boundedness: `vcf adopt` preserves authoritative local `.vcf/project.db` state even when advisory global-registry sync fails, and emits an explicit warning instructing re-run to heal the registry rather than failing ambiguously.
- (stage 6, info) Failure-isolation / degraded-mode correctness: `review_execute` now resolves endpoint via explicit arg or `config.defaults.review.endpoint` and returns `E_VALIDATION` when neither exists, reducing ambiguous misconfiguration behavior under partial config failure.
- (stage 7, info) Artifact-class gate applied: this repo is primarily a developer CLI/tool with an MCP server component, so Stage 1 was evaluated on tool operational intent and maintainership clarity rather than service-grade pager/SLO criteria.
- (stage 7, info) Operational identity in diff is clear enough for later stages: `vcf adopt`/`project_init_existing` adopt pre-existing directories into VCF tracking; local `.vcf/project.db` remains authoritative when global registry sync fails; failed `ship_release` does not transition state.
- (stage 7, info) State durability risk remains bounded to user-operated SQLite files. Migration v2 (`project.adopted`) is additive and does not change the core ownership boundary: local project DB is authoritative; global registry write failures are explicitly tolerated and recoverable by re-running adopt.

### release_confidence

- (stage 1, info) Schema migration v2 (`project.adopted`) is additive and CHANGELOG now documents rollback expectations for developer-tool recovery (manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup). Later stages should treat rollback as user-operated package/database recovery, not fleet rollback.
- (stage 2, info) No Stage 2 blocking architecture contradiction found in this diff. Main runtime assumptions are explicit: allowed-roots enforcement now matches across CLI and MCP surfaces, review endpoint defaults are config-validated, and no new queue/cache/network service dependency was introduced.
- (stage 3, info) No Stage 3 blocking telemetry-theater issue found in scoped changes. `review_execute` endpoint resolution now fails explicitly when neither explicit endpoint nor `config.defaults.review.endpoint` is present, reducing ambiguous operator failure modes; no misleading health/readiness surface was added in this diff.
- (stage 4, info) No Stage 4 blocking bounded-resource issue found in scoped changes. `review_execute` now resolves endpoint/model via config defaults and can pass endpoint-scoped `provider_options` (for example larger Ollama `num_ctx`) without introducing unbounded task creation, queue growth, or missing timeout surfaces in the changed code.
- (stage 4, info) Capacity realism remains partly claim-based for large-context review flows: comments mention higher Ollama context enabling fuller prompts, but this diff adds no benchmark or sustained-load evidence. Treat as accepted confidence limitation rather than a gating defect for this CLI-focused release.
- (stage 5, info) Repeatability/operator-memory risk: bounded in scoped diff. `vcf adopt` is fully parameterized in CLI code with explicit config-load/allowed-roots failure modes, and `review_execute` endpoint resolution now follows explicit arg -> config.defaults.review.endpoint -> E_VALIDATION rather than relying on hidden per-call operator memory.
- (stage 5, info) Config/parity drift risk: reduced by introducing schema-validated `config.defaults` with cross-reference checks to declared endpoints. Per-step endpoint/model behavior is now controlled in `config.yaml` rather than ad hoc invocation arguments, but later stages should remember behavior can still change via config release, not only code release.
- (stage 5, info) Migration/state-change rollback risk: migration v2 remains additive (`project.adopted`) and CHANGELOG now documents user-operated recovery for downgrade scenarios (manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup). No down-migration ships; acceptable for current CLI artifact but remains a forward-only bias to note in final release judgment.
- (stage 5, info) Readiness/health-signal trust risk: no new misleading health/readiness surface was introduced in this diff. For the MCP side, `review_execute` now fails explicitly when required endpoint defaults are absent instead of proceeding ambiguously.
- (stage 5, info) AI-native release-control risk: `review_execute` behavior can now materially change through `config.defaults.review.*` and endpoint `provider_options` without code edits. This is controlled and schema-validated, but Production 9/Security 9 should continue treating config changes to model/endpoint/options as release-affecting artifacts.
- (stage 6, info) Restart / idempotency / recovery risk remains bounded in scoped changes: `ship_release` only transitions project state to `shipped` after successful `gh` completion; failed release attempts leave project state unchanged, avoiding false-success state corruption on dependency failure.
- (stage 6, info) Confidence limitation for later stages: endpoint-scoped `provider_options` can materially change LLM behavior (for example Ollama context length) through config without code edits. This is schema-validated and scoped per endpoint, but later production/security release judgment should continue treating such config changes as release-affecting runtime behavior.
- (stage 7, info) Schema migration v2 (`project.adopted`) is additive and CHANGELOG now documents rollback expectations for developer-tool recovery (manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup). Later stages should treat rollback as user-operated package/database recovery, not fleet rollback.
- (stage 7, info) No Stage 2 blocking architecture contradiction found in this diff. Main runtime assumptions are explicit: allowed-roots enforcement now matches across CLI and MCP surfaces, review endpoint defaults are config-validated, and no new queue/cache/network service dependency was introduced.
- (stage 7, info) No Stage 3 blocking telemetry-theater issue found in scoped changes. `review_execute` endpoint resolution now fails explicitly when neither explicit endpoint nor `config.defaults.review.endpoint` is present, reducing ambiguous operator failure modes; no misleading health/readiness surface was added in this diff.
- (stage 7, info) No Stage 4 blocking bounded-resource issue found in scoped changes. `review_execute` now resolves endpoint/model via config defaults and can pass endpoint-scoped `provider_options` (for example larger Ollama `num_ctx`) without introducing unbounded task creation, queue growth, or missing timeout surfaces in the changed code.
- (stage 7, info) Repeatability/operator-memory risk: bounded in scoped diff. `vcf adopt` is fully parameterized in CLI code with explicit config-load/allowed-roots failure modes, and `review_execute` endpoint resolution now follows explicit arg -> config.defaults.review.endpoint -> E_VALIDATION rather than relying on hidden per-call operator memory.
- (stage 7, info) Config/parity drift risk: reduced by introducing schema-validated `config.defaults` with cross-reference checks to declared endpoints. Per-step endpoint/model behavior is now controlled in `config.yaml` rather than ad hoc invocation arguments, but later stages should remember behavior can still change via config release, not only code release.
- (stage 7, info) Migration/state-change rollback risk: migration v2 remains additive (`project.adopted`) and CHANGELOG now documents user-operated recovery for downgrade scenarios (manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup). No down-migration ships; acceptable for current CLI artifact but remains a forward-only bias to note in final release judgment.
- (stage 7, info) Readiness/health-signal trust risk: no new misleading health/readiness surface was introduced in this diff. For the MCP side, `review_execute` now fails explicitly when required endpoint defaults are absent instead of proceeding ambiguously.
- (stage 7, info) AI-native release-control risk: `review_execute` behavior can now materially change through `config.defaults.review.*` and endpoint `provider_options` without code edits. This is controlled and schema-validated, but Production 9/Security 9 should continue treating config changes to model/endpoint/options as release-affecting artifacts.
- (stage 7, info) Restart / idempotency / recovery risk remains bounded in scoped changes: `ship_release` only transitions project state to `shipped` after successful `gh` completion; failed release attempts leave project state unchanged, avoiding false-success state corruption on dependency failure.

