---
type: review-report
review_type: production
stage: 7
verdict: NEEDS_WORK
run_id: production-7-20260422T005221485Z
created_at: 2026-04-22T00:53:15.113Z
---

# production — Stage 7 — NEEDS_WORK

## Summary

The diff introduces significant new operational surfaces—specifically the `vcf adopt` command and a new `config.yaml` defaults block—but provides zero updated operational documentation. While the code is robust (including a fix for a critical production path in `templatesDir`), the 'Human Supportability' bar is not met because the new features are undocumented for a non-author operator. An operator inheriting this tonight would not know how to use `adopt` to recover a project or how to configure the new tool-specific defaults without reading the source code.

## Findings

- **warning** — `src/cli.ts:787` — The `vcf adopt` command is a high-impact operational tool (bypassing standard scaffolding). However, there is no corresponding update to the project's operational runbooks or README to explain when to use this versus `init`, or how to handle the resulting 'adopted' state in the project DB.
  - required: Add a section to the operational docs/runbook explaining the `adopt` workflow, specifically for recovery or migration of existing projects.
- **warning** — `src/config/schema.ts:205` — The new `DefaultsSchema` allows per-tool endpoint/model overrides. While `vcf init` adds a commented example to the seed file, this is 'setup documentation' not 'operational documentation'. There is no durable record of the resolution order (explicit -> default -> legacy) for an operator troubleshooting why a specific model is being used during a production review run.
  - required: Document the model/endpoint resolution logic in the operational guide or a dedicated config reference doc.
- **info** — `src/util/templates.ts:33` — The fix for `templatesDir()` correctly addresses a production bundling issue (ENOENT). This is a high-value operational fix, but the 'smoke-test coverage' mentioned in the CHANGELOG as a regression guard should be explicitly linked to a test file to ensure the on-call engineer knows how to verify this specific path in the future.
  - required: Ensure the regression guard test is explicitly named in the CHANGELOG or a runbook 'Verification' section.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 7, warning) New operational capabilities (`adopt` and `config.defaults`) are currently trapped as tribal knowledge/source-code comments. Supportability is fragile until these are moved into durable runbooks.

### release_confidence

- (stage 7, info) High confidence in the technical fix for template resolution; however, operational confidence is lowered by the lack of documentation for the new CLI surface.

