---
type: review-report
review_type: production
stage: 3
verdict: BLOCK
run_id: production-3-20260422T004909326Z
created_at: 2026-04-22T00:49:27.381Z
---

# production — Stage 3 — BLOCK

## Summary

Observability posture is weak for the new operational surfaces introduced in this diff. The change adds a new CLI adoption flow, a new MCP tool registration path, new config-driven routing behavior for review execution, and a project-state transition on release, but the diff shows no corresponding metrics, structured logging improvements, alert definitions, health/readiness semantics, or runbook/dashboard updates. Critical-path coverage confidence is low: failures in adopt/release state mutation and default-endpoint resolution would be diagnosed primarily from ad hoc CLI stderr or test behavior, not durable operational signals. Biggest signal gaps: no instrumentation for new command/tool outcomes, no telemetry on config-default fallback behavior, no evidence of actionable alerts or dashboards for release/adoption failures, and no health-signal evidence at all for these changed paths.

## Findings

- **blocker** — `src/cli.ts:787` — New operational surface `vcf adopt` is added without any observable service-level telemetry beyond human-facing CLI output. The command creates or mutates `.vcf/project.db`, writes registry state, preserves or sets project state, and can fail across filesystem and DB boundaries, but the diff adds no structured logs, metrics, trace/correlation hooks, alerting hooks, or runbook references for diagnosing adoption failures at scale or in unattended environments.
  - required: Add trustworthy operational evidence for adopt outcomes and failures: at minimum structured success/failure logging with project root/slug/state context, counters for adopt attempts/successes/failures by failure class, and documentation of where operators inspect these signals.
- **blocker** — `src/tools/review_execute.ts:97` — `review_execute` now changes endpoint/model resolution semantics to depend on `config.defaults.review`, but there is no telemetry indicating which resolution path was used, when default routing is active, or when defaulted endpoint/model selections are causing errors or degraded behavior. This is a critical review-execution path change with no added metrics/tracing/logging evidence, making partial failures and misconfiguration hard to detect or diagnose.
  - required: Emit structured operational signal for endpoint/model resolution and failure modes: record whether explicit/default/legacy resolution was used, count validation failures due to missing defaults, and expose endpoint/model selection in logs/metrics/traces without leaking secrets.
- **blocker** — `src/tools/ship_release.ts:240` — Successful releases now mutate `project.state` to `shipped` and attempt to mirror that to the global registry, but the diff adds no operational evidence for this state-transition path. The global registry update intentionally swallows errors as non-fatal, which creates a silent divergence risk between authoritative project state and registry cache with no log, metric, or alertable signal shown in the diff.
  - required: Instrument the release-state transition path. At minimum log successful and failed registry mirroring with project identifier/context, emit a metric for mirror failures, and document operator-visible evidence for reconciling cache divergence after release.
- **warning** — `src/server.ts:158` — A new MCP tool `project_init_existing` is registered, but the diff shows no observability contract for the tool itself—no metrics, tracing, or documentation references establishing how operators detect failures or usage patterns for this newly exposed surface.
  - required: Add tool-level telemetry and/or documentation pointing to existing telemetry for MCP tool invocation success/failure, latency, and validation errors.
- **warning** — `CHANGELOG.md:8` — The changelog claims operator-facing behavior changes such as adopting existing projects, automatic DB migration, and endpoint/model default routing, but the diff does not provide corresponding observability artifacts or references. This creates telemetry/documentation theater risk: operators are told the features exist and auto-migrate, but are not told how to observe or verify them during incidents.
  - required: Add operational documentation or links for how operators verify adoption, migration, default routing, and release-state transitions in practice.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 3, blocker) Logging/actionability gap: new `vcf adopt`, `project_init_existing`, and `ship_release` state-mutation paths lack durable operational evidence beyond local CLI/test behavior. Operators cannot reliably detect or diagnose failures without guessing.
- (stage 3, blocker) Metrics/tracing gap: `review_execute` default endpoint/model routing introduces a new critical-path decision point with no visible metrics or correlation signal showing whether explicit/default/legacy resolution was used or failed.
- (stage 3, info) AI/model telemetry gap: `review_execute` now defaults model and endpoint from config, but no signal is added for default-routed model/provider usage, which limits later diagnosis of model-specific latency/error regressions.

### release_confidence

- (stage 3, blocker) Health-signal truthfulness risk: this diff provides no health/readiness or monitoring evidence for new operational paths, so later rollout/release stages should not assume these changes are observable in production.
- (stage 3, warning) Alert-fatigue / non-actionable-signal risk: there is no evidence of alerting at all for the changed paths; severe failures may be completely silent rather than noisy, weakening release confidence.

