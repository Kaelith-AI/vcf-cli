---
type: review-report
review_type: production
stage: 3
verdict: PASS
run_id: production-3-20260422T015053560Z
created_at: 2026-04-22T01:51:00.775Z
---

# production — Stage 3 — PASS

## Summary

The diff introduces project adoption functionality and per-step defaults, with proper validation and rollback documentation. No critical observability gaps found; logging and error handling are adequate for a CLI tool. The artifact class is correctly identified as a developer CLI, not a service, so service-grade observability requirements don't apply.

## Findings

- **info** — `src/cli.ts:790` — New 'adopt' CLI command added for adopting existing project directories into VCF tracking
  - required: No change required
- **info** — `src/config/schema.ts:210` — Added DefaultsSchema for per-step model/endpoint defaults in config.yaml
  - required: No change required
- **info** — `src/db/schema.ts:198` — Project DB migration v2 adds 'adopted' column to track projects brought in via project_init_existing
  - required: No change required
- **info** — `src/tools/review_execute.ts:45` — Endpoint field in review_execute input schema now optional with description
  - required: No change required
- **info** — `src/tools/ship_release.ts:246` — Ship release now transitions project.state to 'shipped' on successful release
  - required: No change required
- **info** — `src/util/templates.ts:36` — Fixed templatesDir() to resolve correctly in both dev and bundled layouts
  - required: No change required

## Carry-forward

### architecture

- (stage 2, info) Deploy-shape truth: artifact remains a local developer CLI plus MCP server process, not a standalone production service. New `vcf adopt` / `project_init_existing` flows continue to use per-project SQLite in `<project>/.vcf/project.db` as authoritative state, with `~/.vcf/vcf.db` as advisory registry cache.

### verification

_none_

### security

_none_

### compliance

_none_

### supportability

- (stage 1, info) Artifact-class gate applied: this repo is primarily a developer CLI/tool with an MCP server component, so Stage 1 was evaluated on tool operational intent and maintainership clarity rather than service-grade pager/SLO criteria.
- (stage 1, info) Operational identity in diff is clear enough for later stages: `vcf adopt`/`project_init_existing` adopt pre-existing directories into VCF tracking; local `.vcf/project.db` remains authoritative when global registry sync fails; failed `ship_release` does not transition state.
- (stage 2, info) State durability risk remains bounded to user-operated SQLite files. Migration v2 (`project.adopted`) is additive and does not change the core ownership boundary: local project DB is authoritative; global registry write failures are explicitly tolerated and recoverable by re-running adopt.
- (stage 3, info) Artifact-class gate applied: this repo is primarily a developer CLI/tool with an MCP server component, so Stage 1 was evaluated on tool operational intent and maintainership clarity rather than service-grade pager/SLO criteria.
- (stage 3, info) Operational identity in diff is clear enough for later stages: 'vcf adopt'/'project_init_existing' adopt pre-existing directories into VCF tracking; local '.vcf/project.db' remains authoritative when global registry sync fails; failed 'ship_release' does not transition state.

### release_confidence

- (stage 1, info) Schema migration v2 (`project.adopted`) is additive and CHANGELOG now documents rollback expectations for developer-tool recovery (manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup). Later stages should treat rollback as user-operated package/database recovery, not fleet rollback.
- (stage 2, info) No Stage 2 blocking architecture contradiction found in this diff. Main runtime assumptions are explicit: allowed-roots enforcement now matches across CLI and MCP surfaces, review endpoint defaults are config-validated, and no new queue/cache/network service dependency was introduced.
- (stage 3, info) Schema migration v2 ('project.adopted') is additive and CHANGELOG now documents rollback expectations for developer-tool recovery (manual DROP COLUMN on SQLite 3.35+ or restore SQLite files from backup). Later stages should treat rollback as user-operated package/database recovery, not fleet rollback.

