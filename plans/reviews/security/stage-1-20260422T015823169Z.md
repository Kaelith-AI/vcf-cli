---
type: review-report
review_type: security
stage: 1
verdict: PASS
run_id: security-1-20260422T015815108Z
created_at: 2026-04-22T01:58:23.169Z
---

# security — Stage 1 — PASS

## Summary

Project introduces 'vcf adopt' for integrating existing directories into VCF tracking, adds per-tool model/endpoint defaults, and improves Ollama context handling. Security scope is clear: assets include project state DBs, config files, and LLM prompts; trust boundaries are explicit around local endpoints, global registry, and operator-controlled paths. Obligations are minimal but documented, with no compliance violations. Key assumptions are explicit and testable.

## Findings

- **info** — `src/cli.ts:790` — The 'vcf adopt' CLI command now enforces allowed_roots from config.yaml, matching the MCP tool's behavior for consistency and safety.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/review_execute.ts:45` — The review_execute tool now supports optional endpoint argument with fallback to config.defaults.review.endpoint, improving configurability.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/review_execute.ts:154` — Provider-specific options (providerOptions) are now passed through to LLM endpoints, enabling better control over Ollama context limits.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/ship_release.ts:246` — Release output (stdout/stderr) is now redacted before being included in audit logs, preventing accidental exposure of secrets.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/ship_release.ts:274` — On successful release, project.state transitions to 'shipped' and is mirrored in the global registry, enabling portfolio queries to distinguish shipped from reviewing projects.
  - required: No change required; this is a security improvement.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

- (stage 1, info) Assets: project.db, vcf.db, config.yaml, LLM prompts, and tool outputs. Trust boundaries: local endpoints, global registry, operator-controlled paths. Actor classes: operator, MCP tool, external LLM providers. Obligations: minimal, no compliance violations identified.

### compliance

- (stage 1, info) No explicit compliance obligations visible in this diff. Project handles user data through project DBs and config files; no PII or sensitive data handling identified.

### supportability

- (stage 1, info) Project state transitions are now tracked in project.db and mirrored in global registry. Migration v2 adds 'adopted' column with documented rollback path.

### release_confidence

_none_

