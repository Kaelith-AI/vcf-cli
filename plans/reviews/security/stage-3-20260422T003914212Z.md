---
type: review-report
review_type: security
stage: 3
verdict: PASS
run_id: security-3-20260422T003822618Z
created_at: 2026-04-22T00:39:14.212Z
---

# security — Stage 3 — PASS

## Summary

The changes introduce a new 'adopt' mechanism for existing projects, a configuration system for per-tool LLM defaults, and a fix for template resolution in bundled environments. From a security architecture perspective, the boundaries are well-enforced. The 'adopt' command correctly validates input paths and restricts project states to a known allowlist. The LLM endpoint resolution logic in `review_execute` maintains existing trust-level gating (public vs. private) while allowing flexible configuration. The state transition to 'shipped' is correctly gated on the success of the external release process. No architectural compromises or phantom boundaries were identified.

## Findings

- **info** — `src/cli.ts:812` — The `runAdopt` function uses `absRoot.split('/').pop()` to derive a default project name. While not a security vulnerability in a CLI tool running in a user's local environment, it is a naive string operation for name derivation.
  - required: None required; acceptable for local CLI usage.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

- (stage 3, info) The 'adopt' feature allows VCF to track projects not born in VCF. Future 'strict' or 'reconstruct' modes mentioned in the DB migration should be reviewed for boundary enforcement when implemented.

### compliance

_none_

### supportability

_none_

### release_confidence

- (stage 3, info) Template resolution fix in `src/util/templates.ts` resolves a critical path for bundled installs; verification is confirmed via the updated logic that walks up to package.json.

