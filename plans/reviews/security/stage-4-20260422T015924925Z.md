---
type: review-report
review_type: security
stage: 4
verdict: PASS
run_id: security-4-20260422T015916181Z
created_at: 2026-04-22T01:59:24.925Z
---

# security — Stage 4 — PASS

## Summary

The implementation demonstrates secure coding practices across input validation, injection resistance, authentication, crypto usage, error handling, and resource safety. Key improvements include proper endpoint resolution with config defaults, secure handling of provider options, and state transitions gated on successful release. No critical or high-severity issues found in the implementation paths reviewed.

## Findings

- **info** — `src/cli.ts:800` — The runAdopt function now enforces allowed_roots configuration check, matching the MCP tool's behavior and preventing path traversal outside configured boundaries.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/review_execute.ts:100` — Endpoint resolution now falls back to config.defaults.review.endpoint when omitted, providing a more flexible and secure configuration path.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/review_execute.ts:154` — Provider-specific options are now passed through to the LLM client, enabling proper configuration for endpoints like Ollama without leaking configuration to other providers.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/ship_release.ts:246` — stdout/stderr from subprocess calls are now redacted before being surfaced or audited, reducing information leakage risk.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/ship_release.ts:275` — Project state transitions to 'shipped' only on successful release, preventing incorrect state updates on failed releases.
  - required: No change required; this is a security improvement.
- **info** — `src/util/templates.ts:33` — The templatesDir function now uses a walk-up algorithm to resolve the package root, fixing a production vs development path resolution issue.
  - required: No change required; this is a security improvement.

## Carry-forward

### architecture

_none_

### verification

- (stage 3, warning) Server wiring adds registerProjectInitExisting in src/server.ts:158, but the scoped diff omits src/tools/project_init_existing.ts. Later stages should confirm that tool's live path enforces the same allowed_roots and mediation guarantees claimed in README/response-log context, since Stage 3 should verify implementation rather than rely on registration/docs alone.

### security

- (stage 3, info) CLI adopt path now mirrors MCP allowed_roots enforcement in src/cli.ts:787 and fails closed on malformed config, reducing CLI/MCP boundary asymmetry previously flagged at security stage 2.
- (stage 3, info) review_execute retains endpoint trust boundary enforcement after adding config defaults: endpoint still resolves from declared config and public endpoints remain blocked unless allow_public_endpoint is set (src/tools/review_execute.ts:97-115).
- (stage 3, info) ship_release now redacts stdout/stderr before surfacing or auditing subprocess tails (src/tools/ship_release.ts:243-271), reducing leakage risk across the tool-to-audit/output boundary.
- (stage 4, info) The project_init_existing tool and CLI adopt command now enforce consistent allowed_roots boundaries between MCP and CLI surfaces, reducing potential attack surface from path traversal.
- (stage 4, info) Endpoint resolution now supports config defaults, improving configurability while maintaining security through explicit validation.
- (stage 4, info) Provider-specific options are now properly handled and passed through to LLM clients, preventing unintended configuration leakage.
- (stage 4, info) State transitions in ship_release are now gated on successful subprocess completion, preventing incorrect state updates.
- (stage 4, info) Output redaction in ship_release prevents leakage of sensitive information from subprocess calls.

### compliance

- (stage 4, info) The project database migration v2 includes an adopted flag for tracking project adoption status, which is compliant with the project's data handling requirements.

### supportability

_none_

### release_confidence

- (stage 3, info) No Stage-3 blocker found in the visible diff. Residual risk accepted: direct verification of the new MCP adopt tool implementation is deferred because its source is outside the scoped diff.

