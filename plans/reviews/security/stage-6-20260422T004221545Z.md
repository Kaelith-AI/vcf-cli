---
type: review-report
review_type: security
stage: 6
verdict: BLOCK
run_id: security-6-20260422T004124768Z
created_at: 2026-04-22T00:42:21.545Z
---

# security — Stage 6 — BLOCK

## Summary

A critical security vulnerability was discovered in `src/util/templates.ts` where a constant `PACKAGE_ROOT` is assigned a value that appears to be a hardcoded secret or a redacted placeholder that was accidentally committed to source. Additionally, the logic for resolving the package root has been fundamentally broken by the introduction of this constant, bypassing the dynamic resolution logic intended to fix a production path issue.

## Findings

- **blocker** — `src/util/templates.ts:35` — The constant `PACKAGE_ROOT` is assigned a value `[REDACTED]`. In the context of a security review, this indicates either a hardcoded secret was committed and then redacted by the MCP, or a placeholder was committed to source. If this is a real value in the source, it is a direct violation of the 'No hardcoded secrets' rule. Furthermore, the `templatesDir()` function now uses this constant instead of the `findPackageRoot` helper, rendering the `findPackageRoot` logic dead code and the path resolution broken.
  - required: Remove the `PACKAGE_ROOT` constant and restore the call to `findPackageRoot(__dirname)` within `templatesDir()` to ensure dynamic and safe path resolution.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

- (stage 6, blocker) Critical failure in secret management and path resolution in `src/util/templates.ts`. The use of a hardcoded constant for a system path (or secret) blocks release.

### compliance

_none_

### supportability

_none_

### release_confidence

_none_

