---
type: review-report
review_type: security
stage: 2
verdict: PASS
run_id: security-2-20260422T011921771Z
created_at: 2026-04-22T01:20:03.737Z
---

# security — Stage 2 — PASS

## Summary

The diff introduces a new 'adopt' flow for existing projects, per-step LLM defaults in config, and a fix for template resolution in bundled installs. From a threat-modeling perspective, the most significant change is the `vcf adopt` / `project_init_existing` surface. This surface is now correctly aligned with the MCP server's trust boundaries by implementing `assertInsideAllowedRoot` in the CLI, preventing the tool from being used to register arbitrary filesystem paths outside the operator's defined workspace. The LLM configuration changes are purely additive and do not introduce new trust boundaries or input vectors. The template resolution fix is a logic correction with no security impact. No high-severity unmitigated abuse paths were identified.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

- (stage 2, info) The `vcf adopt` flow relies on the operator's local filesystem permissions. While `allowed_roots` provides a logical boundary, the tool operates with the privileges of the user running the CLI. This is the accepted threat model for a developer CLI.

### compliance

- (stage 2, info) The addition of the `adopted` flag in the project database provides a provenance marker that can be used by future auditing tools to distinguish between VCF-scaffolded and adopted projects.

### supportability

_none_

### release_confidence

_none_

