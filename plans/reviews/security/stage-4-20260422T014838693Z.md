---
type: review-report
review_type: security
stage: 4
verdict: PASS
run_id: security-4-20260422T014829051Z
created_at: 2026-04-22T01:48:38.693Z
---

# security — Stage 4 — PASS

## Summary

Reviewed the new secure-coding paths in CLI adoption, review endpoint/default resolution, provider option pass-through, ship_release output/state handling, schema validation, and template path resolution. No High/Medium implementation-security issue is evidenced in the scoped diff. Untrusted operator CLI path input is now constrained against allowed_roots when config exists; review_execute still enforces declared-endpoint lookup and trust-level gating after adding defaults fallback; ship_release now redacts subprocess stdout/stderr before envelope/audit storage and only flips project state to shipped on successful gh exit. Residual risk remains that config.yaml is trusted administrative input, so endpoint.provider_options can still influence outbound provider request bodies by design.

## Carry-forward

### architecture

_none_

### verification

- (stage 1, info) Later stages should verify boundary claims, not just comments: (1) project_init_existing MCP tool mirrors allowed_roots enforcement claimed by CLI parity comment in src/cli.ts:800-805; (2) review_execute honors trust-level gating when endpoint is sourced from config.defaults.review.endpoint in src/tools/review_execute.ts:100-118; (3) provider_options cannot bypass expected request-shaping assumptions in src/config/schema.ts:80-87,207-244 and src/util/llmClient.ts:75-91.
- (stage 2, info) Security Stage 3/4 should verify that the boundary claims relied on here are real, not comment-level: review_execute must continue enforcing allow_public_endpoint against defaults.review.endpoint as well as explicit endpoint input (src/tools/review_execute.ts:97-118), and provider_options passthrough must not enable request-shape confusion beyond intended provider behavior (src/config/schema.ts:80-87,207-244; src/util/llmClient.ts:88-98).
- (stage 3, info) Tests now cover the boundary-sensitive review_execute cases: defaults fallback, explicit override precedence, provider_options pass-through, omission when unset, and validation failure when no endpoint/default exists (test/integration/review_execute.test.ts:330-484).
- (stage 4, info) Negative-path tests cover review_execute default endpoint/model fallback, explicit override precedence, provider_options inclusion/omission, and validation failure when no endpoint/default exists (test/integration/review_execute.test.ts:330-484). Failed-release test confirms project.state does not advance on non-zero gh exit (test/integration/ship_release.test.ts:141-175).

### security

- (stage 1, info) High-value assets in scope: per-project state in <project>/.vcf/project.db, global registry in ~/.vcf/vcf.db, config.yaml endpoint/default selections, review prompts/responses sent to configured providers, and ship_release stdout/stderr persisted to envelope/audit after redaction.
- (stage 1, info) Major trust boundaries: operator/CLI path input crossing into filesystem writes in src/cli.ts:790-819; MCP review execution crossing into external model providers via configured endpoints in src/tools/review_execute.ts:100-173 and src/util/llmClient.ts:75-91; subprocess boundary to gh in src/tools/ship_release.ts:181-216 with output crossing into audit/envelope at src/tools/ship_release.ts:246-301.
- (stage 1, info) Actor/privilege map for later stages: operator invoking CLI commands; MCP client invoking review/ship tools; configured model providers as external semi-trusted processors; local/global SQLite stores as persistence assets; gh subprocess as external tool execution boundary. Later review should confirm no actor classes are implicitly collapsed in the new project_init_existing MCP tool.
- (stage 2, info) Stage 2 abuse-path summary: most plausible threats remain operator/admin misconfiguration of config.defaults.review and endpoint.provider_options, plus external model-provider exposure of review content. Current diff keeps endpoint selection behind declared-endpoint validation and existing trust-level checks in src/tools/review_execute.ts:97-118; no new direct prompt-to-tool privilege chain is added.
- (stage 2, info) Mandatory AI-category status for downstream stages: prompt injection MITIGATED for this diff surface because review_execute still sends prompts to a model but does not grant model-driven tool invocation here (src/tools/review_execute.ts:151-177); tool misuse / agent permission escalation / infrastructure escalation MITIGATED in scoped changes because no new agentic execution path is introduced; data exfiltration via model/provider trust boundary PARTIAL because review content still leaves the host for configured providers by design (src/util/llmClient.ts:75-101); hallucinated capability trust PARTIAL because downstream consumers still depend on review JSON validation rather than model truthfulness, which later stages should verify.
- (stage 3, info) Boundary enforcement for adopted-project creation is now aligned across CLI and MCP surfaces: runAdopt refuses out-of-scope paths when config exists and only skips the check for explicit pre-init/no-config cases (src/cli.ts:804-821). Later stages can treat allowed_roots parity as implemented for this diff.
- (stage 3, info) review_execute still enforces declared-endpoint lookup and blocks public endpoints unless allow_public_endpoint is set, even after adding defaults.review.endpoint fallback (src/tools/review_execute.ts:100-118). No new authz/trust-gate bypass is visible in the fallback path.
- (stage 3, info) Provider-specific request shaping remains mediated by trusted config, not caller input: endpoint.provider_options is validated as part of config loading and only attached from the resolved endpoint to outbound LLM requests (src/config/schema.ts:80-87,287-322; src/tools/review_execute.ts:159-177; src/util/llmClient.ts:88-98). Residual risk accepted from prior stages remains that config.yaml is trusted administrative input.
- (stage 4, info) Stage 4 secure-coding check found no new exploitable injection/auth/crypto/info-leak issue in the scoped diff. review_execute continues to enforce declared-endpoint lookup and allow_public_endpoint gating even when endpoint is sourced from config.defaults.review.endpoint (src/tools/review_execute.ts:97-118).
- (stage 4, info) Provider-specific request shaping is still trusted-admin config, not caller input: endpoint.provider_options is schema-validated only as an unconstrained record and is merged into outbound LLM request bodies as body.options (src/config/schema.ts:80-87,287-322; src/tools/review_execute.ts:151-177; src/util/llmClient.ts:88-98). Residual risk accepted: a config editor can alter provider behavior, but this is within the existing trusted-config threat model.
- (stage 4, info) ship_release now redacts gh stdout/stderr before returning expanded content or persisting audit content, reducing secret/sensitive output leakage on this subprocess boundary (src/tools/ship_release.ts:243-271). State transition to shipped is correctly gated on successful gh exit (src/tools/ship_release.ts:274-289).

### compliance

- (stage 1, info) Obligation signals: outbound model calls may process review content under third-party provider policies; command output may contain secret-shaped or sensitive material, so audit/envelope redaction in src/tools/ship_release.ts:246-301 is security/privacy relevant. No explicit regulated-data regime is asserted in this diff.

### supportability

_none_

### release_confidence

- (stage 1, info) Assumptions requiring later verification: config.yaml is trustworthy administrative input; allowed_roots meaningfully constrains adoption blast radius; external providers tolerate/handle body.options as intended; redact() adequately scrubs sensitive subprocess output before storage. These assumptions are explicit enough for downstream security stages to test.
- (stage 2, info) Bounded residual risk accepted at this stage: config.yaml is treated as trusted administrative input. If an attacker can modify config defaults or endpoint definitions, they can redirect review traffic or alter provider behavior; that is a host/config compromise assumption, not a new unauthenticated abuse path introduced by this diff.
- (stage 3, info) Residual risk tolerated: ship_release state mirroring to the global registry remains advisory/non-fatal by design, but this diff does not expose a new external privilege bypass because project state transition to shipped is gated on successful gh exit (src/tools/ship_release.ts:274-289).
- (stage 4, info) Confidence medium-high for this diff scope: secure behavior is enforced in code on the reviewed paths, but provider_options remains a broad pass-through from trusted config to external providers, so later release review should continue treating config.yaml integrity as a core assumption.

