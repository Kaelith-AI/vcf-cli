---
type: review-report
review_type: security
stage: 1
verdict: PASS
run_id: security-1-20260422T080054346Z
created_at: 2026-04-22T08:01:25.724Z
---

# security — Stage 1 — PASS

## Summary

Security scope is now explicit enough for later stages to proceed. Highest-value assets named in code/docs are project-local `.vcf/project.db`, cross-project `~/.vcf/lessons.db`, review/response/decision history, lifecycle-report outputs, and outbound LLM prompt bundles; main entry points are `lesson_log_add`, `lesson_search`, `lifecycle_report`, `review_execute`, `project_init_existing`, and `vcf adopt`.

Trust boundaries are clearly modeled: project DB vs global lessons mirror (`src/db/globalLessons.ts`), project-scope MCP session vs cross-project reads (`README.md` cross-project trust-boundary section), local vs non-local endpoint routing for review and narrative lifecycle reports (`src/tools/review_execute.ts`, `src/tools/lifecycle_report.ts`), and operator-driven CLI adoption vs configured `allowed_roots` (`src/cli.ts`, `src/tools/project_init_existing.ts`). Actor classes are distinguishable: local operator, project-scope MCP caller, global registry/global lessons DB, and external model endpoint/provider.

Obligation signals are visible without inventing regimes: shared-workstation confidentiality concerns for the global lessons mirror are documented; residency/retention sensitivity for narrative lifecycle-report and review routing is called out; redaction is explicitly framed as not equivalent to confidentiality. Core assumptions later stages should test are also named: `config.lessons.global_db_path: null` disables the mirror, non-local defaults-routed endpoints require explicit opt-in, and redaction runs before lesson persistence and before outbound narrative/review prompts.

Confidence: Medium-High. The diff materially improves Stage-1 scope clarity and documents the main trust boundaries instead of leaving them implicit.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

- (stage 1, info) Later security stages should verify the documented mirror-disable boundary end-to-end: `config.lessons.global_db_path: null` must prevent global writes and reject `lesson_search(scope="global"|"all")`, not just document that behavior.
- (stage 1, info) Later security stages should test the non-local endpoint routing gate in both `review_execute` and `lifecycle_report` narrative mode, especially defaults-resolved `trust_level='trusted'` endpoints, to confirm off-host routing always needs explicit operator consent.
- (stage 1, info) Later security stages should verify both redaction surfaces named here: pre-store redaction for lessons/response content and pre-send redaction for narrative lifecycle-report and review prompt bundles.

### compliance

_none_

### supportability

_none_

### release_confidence

- (stage 1, info) Stage 1 scope is clear for the newly added assets and boundaries: cross-project lessons mirror, lifecycle-report outbound narration, and defaults-based model routing can now be reviewed concretely in later stages.

