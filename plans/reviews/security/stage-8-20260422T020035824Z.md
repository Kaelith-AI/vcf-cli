---
type: review-report
review_type: security
stage: 8
verdict: PASS
run_id: security-8-20260422T020026414Z
created_at: 2026-04-22T02:00:35.824Z
---

# security — Stage 8 — PASS

## Summary

The deployment infrastructure and runtime isolation for the vcf-cli project are appropriately controlled for a developer CLI tool. Privilege and exposure posture are reasonably bounded, with no evidence of unsafe defaults or broad compromise paths. Secrets injection and runtime trust are handled safely, with no secrets baked into artifacts. Infrastructure-as-code and IAM practices reflect least-privilege principles. The new `project_init_existing` tool and per-step defaults are implemented with appropriate security controls.

## Findings

- **info** — `src/cli.ts:787` — The `runAdopt` function now enforces `allowed_roots` checking, matching the MCP tool's behavior and preventing arbitrary directory adoption. This ensures that the CLI surface has the same blast-radius guarantee as the MCP tool.
  - required: No change required; the implementation correctly mirrors the MCP tool's safety checks.
- **info** — `src/tools/review_execute.ts:97` — The `review_execute` tool now resolves endpoint via explicit arg → config.defaults.review.endpoint → E_VALIDATION, ensuring that endpoint selection is properly validated and defaults are respected.
  - required: No change required; the implementation correctly implements the default resolution order.
- **info** — `src/tools/review_execute.ts:151` — Per-endpoint `provider_options` are now scoped to the selected endpoint and only attached when configured, avoiding over-broad request-shaping across providers.
  - required: No change required; the implementation correctly scopes provider options to the selected endpoint.
- **info** — `src/tools/ship_release.ts:243` — The `ship_release` tool now redacts stdout/stderr before surfacing or auditing subprocess tails, reducing token/header/secret leakage risk from subprocess output.
  - required: No change required; the implementation correctly redacts sensitive output.
- **info** — `src/config/schema.ts:310` — The config schema now validates that `defaults.<tool>.endpoint` references a declared endpoint, preventing misconfiguration that could lead to invalid endpoint usage.
  - required: No change required; the validation correctly enforces endpoint declaration.
- **info** — `src/util/templates.ts:34` — The `templatesDir()` function now uses a walk-up approach to find the package root, resolving the issue where hardcoded paths failed in production builds.
  - required: No change required; the implementation correctly resolves the template directory in both development and production.

## Carry-forward

### architecture

_none_

### verification

- (stage 3, warning) Server wiring adds registerProjectInitExisting in src/server.ts:158, but the scoped diff omits src/tools/project_init_existing.ts. Later stages should confirm that tool's live path enforces the same allowed_roots and mediation guarantees claimed in README/response-log context, since Stage 3 should verify implementation rather than rely on registration/docs alone.
- (stage 4, warning) Stage 4 reviewed secure coding in the visible CLI/server call paths only. Direct implementation review of `src/tools/project_init_existing.ts` is still deferred because the source remains outside the scoped diff even though it is registered in src/server.ts:158.
- (stage 5, warning) Direct review of the newly registered MCP tool implementation remains deferred because src/tools/project_init_existing.ts is still omitted from the scoped diff even though it is registered in src/server.ts:158. Later stages should confirm that tool's third-party/build interactions, if any, do not widen supply-chain or callback trust beyond the CLI parity already discussed.
- (stage 6, warning) Direct implementation review of `src/tools/project_init_existing.ts` remains deferred because the source is still outside the scoped diff even though it is registered in `src/server.ts:158`. Later stages should verify its concrete secret/config/identity handling rather than infer parity from CLI/docs.
- (stage 7, warning) Stage 7 runtime confidence is bounded by scope omission: `src/tools/project_init_existing.ts` is still not in the diff even though it is registered in `src/server.ts:158`. Later stages should verify its live error handling, disclosure behavior, and any maintenance/debug paths directly rather than infer parity from CLI/docs.

### security

- (stage 3, info) CLI adopt path now mirrors MCP allowed_roots enforcement in src/cli.ts:787 and fails closed on malformed config, reducing CLI/MCP boundary asymmetry previously flagged at security stage 2.
- (stage 3, info) review_execute retains endpoint trust boundary enforcement after adding config defaults: endpoint still resolves from declared config and public endpoints remain blocked unless allow_public_endpoint is set (src/tools/review_execute.ts:97-115).
- (stage 3, info) ship_release now redacts stdout/stderr before surfacing or auditing subprocess tails (src/tools/ship_release.ts:243-271), reducing leakage risk across the tool-to-audit/output boundary.
- (stage 4, info) `runAdopt` in src/cli.ts:787-842 now enforces existence/type checks, validates `--state` against an allowlist, and mirrors MCP `allowed_roots` scoping with fail-closed behavior on malformed config; no new injection or path-boundary issue is visible in the CLI adoption path.
- (stage 4, info) `review_execute` in src/tools/review_execute.ts:97-115 and :151-174 now resolves endpoint/model via explicit arg then config defaults, fails closed with E_VALIDATION when endpoint is still unset, preserves public-endpoint blocking, and passes provider-specific options only from declared endpoint config through src/util/llmClient.ts:88-91.
- (stage 4, info) `ship_release` in src/tools/ship_release.ts:243-299 now redacts stdout/stderr before envelope/audit exposure and only transitions project state to `shipped` after a successful subprocess exit, reducing information leakage and unsafe-state risk on release paths.
- (stage 5, info) Stage 5 scoped diff introduces no new package provenance, lockfile, or build-time supply-chain blocker. review_execute still resolves endpoints only from declared config.endpoints entries, with defaults.review.endpoint cross-checked in src/config/schema.ts:310-320 and resolved at runtime in src/tools/review_execute.ts:97-115.
- (stage 5, info) Per-endpoint provider_options are now accepted by config schema and forwarded only for the selected endpoint (src/config/schema.ts:80-87, src/tools/review_execute.ts:151-174, src/util/llmClient.ts:88-91). This reduces over-broad third-party request shaping versus a global options fan-out, but trust in the target provider remains operator-configured.
- (stage 6, info) No in-scope secret exposure or identity/key-management blocker found at Stage 6. `review_execute` now fails closed when no endpoint/default is set and validates defaults against declared endpoints (`src/tools/review_execute.ts:97-115`, `src/config/schema.ts:310-320`).
- (stage 6, info) `ship_release` now redacts stdout/stderr before envelope and audit persistence, reducing token/header/secret leakage risk from subprocess output (`src/tools/ship_release.ts:243-299`).
- (stage 6, info) Per-endpoint `provider_options` are scoped to the selected endpoint and omitted when unset, which narrows outbound request shaping and avoids broad fan-out across providers (`src/config/schema.ts:80-87`, `src/tools/review_execute.ts:151-174`, `src/util/llmClient.ts:88-91`).
- (stage 7, info) Runtime exposure in the visible diff stays bounded: `review_execute` resolves endpoint via explicit arg or `config.defaults.review.endpoint`, fails closed with `E_VALIDATION` when unset, and continues blocking `trust_level: public` endpoints unless `allow_public_endpoint` is set (`src/tools/review_execute.ts:97-115`).
- (stage 7, info) Security observability/disclosure posture improved for release operations: `ship_release` now redacts subprocess stdout/stderr before returning expanded content or writing audit content, reducing runtime leak risk while preserving sanitized operator signal (`src/tools/ship_release.ts:243-299`).
- (stage 7, info) Per-endpoint `provider_options` are only attached when configured on the selected endpoint, avoiding over-broad request-shaping across providers and reducing accidental runtime incompatibility/noise (`src/tools/review_execute.ts:151-174`, `src/util/llmClient.ts:88-91`).
- (stage 8, info) The new `project_init_existing` tool and per-step defaults are implemented with appropriate security controls, including `allowed_roots` enforcement and proper validation of endpoint references.

### compliance

- (stage 8, info) No compliance obligations are violated by the changes in this diff. The implementation correctly handles secrets injection and runtime trust, with no evidence of hardcoded secrets or unsafe credential handling.

### supportability

- (stage 8, info) The changes improve supportability by providing clearer error messages and better handling of edge cases, such as when no endpoint is provided or when `provider_options` are not set.

### release_confidence

- (stage 3, info) No Stage-3 blocker found in the visible diff. Residual risk accepted: direct verification of the new MCP adopt tool implementation is deferred because its source is outside the scoped diff.
- (stage 4, info) Within the scoped diff, implementation-security checks found no blocker or warning-level issue. Confidence is bounded by omitted source for `project_init_existing` and by the fact that provider_options values are intentionally operator-configured and forwarded as-is to the selected provider endpoint.
- (stage 5, info) Confidence for Stage 5 is medium: the visible diff shows no uncontrolled dependency or callback-trust regression, but no manifest/lockfile changes or vulnerability-scan artifacts were included, so this pass can only assess the in-scope code paths and config surfaces.
- (stage 6, info) Stage 6 confidence is medium-high for the visible surfaces: no in-scope credential leakage or over-broad identity regression is evident, but confidence remains bounded by the omitted `project_init_existing` implementation.
- (stage 7, warning) Dynamic-testing confidence gap: this pass is static-only on the provided diff. No direct DAST evidence was available for malformed-input behavior, rate-limit behavior, or runtime error disclosure on newly affected paths, so release confidence for runtime posture remains medium.
- (stage 8, info) Release confidence is high. The deployment infrastructure and runtime isolation are appropriately controlled, with no evidence of unsafe defaults or broad compromise paths. The changes are well-tested and align with security best practices.

