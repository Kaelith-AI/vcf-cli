---
type: review-report
review_type: security
stage: 9
verdict: BLOCK
run_id: security-9-20260422T004607881Z
created_at: 2026-04-22T00:46:26.179Z
---

# security — Stage 9 — BLOCK

## Summary

Stage 9 release security readiness is not evidenced by this diff. The change set introduces new security-relevant behavior (`adopt`/`project_init_existing`, release-state mutation, endpoint/model default routing) but provides no Stage 1–8 findings summary, no risk-acceptance/ownership artifacts, no detection/response posture evidence, and no carry-forward table connecting prior findings to a release decision. Per Stage 9 rules, confidence cannot be invented when earlier-stage evidence is absent. Release may be acceptable only for tightly bounded internal dogfood after prior-stage security outputs are produced and linked; it is not defensible as a final security-ready release decision on the evidence shown here.

## Findings

- **blocker** — `src/cli.ts:787` — New externally-invocable adoption flow (`vcf adopt`) persists tracking state for arbitrary existing directories, but this Stage 9 package contains no inherited findings, threat summary, or residual-risk ownership for that new trust boundary. Stage 9 requires aggregation of Stages 1–8 before release; inherited carry-forward is empty and no compensating evidence is present.
  - required: Do not approve release from Stage 9 until prior security stages are completed or attached for the adoption flow: assets, trust boundaries, abuse paths, validation controls, and explicit residual-risk ownership for adopting pre-existing projects.
- **blocker** — `src/server.ts:158` — Server registration adds `project_init_existing`, expanding the MCP attack surface, but there is no accompanying Stage 9 evidence that key controls were verified or that earlier-stage findings for this tool were resolved. A new privileged tool without cumulative security evidence makes the final release judgment undefendable.
  - required: Provide prior-stage review outputs and verified control evidence for `project_init_existing` before release approval. If not reviewed, narrow release scope explicitly to internal testing only and re-run Stage 9 with documented ownership and conditions.
- **blocker** — `src/tools/review_execute.ts:97` — Endpoint selection now silently falls back to `config.defaults.review.endpoint` and model to `config.defaults.review.model`, changing execution-routing semantics for review calls. This is security-relevant because trust-level and provider-routing decisions move from explicit invocation into ambient configuration. Stage 9 includes no evidence of cumulative review for misrouting, config abuse, or operator accountability.
  - required: Carry forward earlier-stage analysis for config-driven endpoint/model routing, including who owns default endpoint trust decisions, what prevents unsafe defaults, and what release scope is acceptable if misconfiguration remains a realistic risk.
- **warning** — `test/integration/ship_release.test.ts:141` — Release-state transition to `shipped` is only negatively tested here; the comment states the positive path is verified by a real release rather than automated test evidence. Stage 9 requires key controls to be evidenced, not asserted by operational intent. For a release gate, relying on an upcoming real tag push is weak verification.
  - required: Add an automated positive-path test or attach verifiable evidence showing successful release transitions update both project and registry state correctly, and record any residual consistency risk if global registry mirroring remains best-effort.
- **blocker** — `CHANGELOG.md:8` — The release narrative claims dogfood readiness and multiple behavior changes, but there is no required Stage 9 findings carry-forward table, no explicit release-security verdict tied to Stages 1–8, and no blocking/conditional requirements with owners/deadlines. This is exactly the optimism-driven drift Stage 9 is meant to stop.
  - required: Produce the Stage 9 release-security record with: earlier-stage findings summary, control verification summary, residual risk ownership, detection/response/patch posture, required carry-forward table, and explicit release-scope decision.

## Carry-forward

### architecture

_none_

### verification

- (stage 9, warning) `ship_release` success-path state transition lacks automated positive-path evidence in this diff; release-critical control verification remains incomplete.

### security

- (stage 9, blocker) Stage 9 cannot pass: no prior security-stage findings or carry-forward entries are present despite new attack surface (`project_init_existing`/`adopt`) and new config-driven routing behavior.
- (stage 9, warning) `review_execute` now depends on ambient config defaults for endpoint/model routing; release decision must explicitly account for trust-level ownership and misconfiguration risk.

### compliance

_none_

### supportability

- (stage 9, warning) No evidence provided in this Stage 9 package for security contact, incident response, alerting, or dependency patch posture tied to the intended dogfood release.

### release_confidence

- (stage 9, blocker) Appropriate release scope is at most limited internal dogfood pending completion/attachment of Stages 1–8 outputs, residual-risk ownership, and the required Stage 9 carry-forward table.

