---
type: review-report
review_type: security
stage: 2
verdict: PASS
run_id: security-2-20260422T015836583Z
created_at: 2026-04-22T01:58:43.914Z
---

# security — Stage 2 — PASS

## Summary

This diff introduces project adoption capabilities and per-tool defaults, with improved config validation and audit redaction. The threat model is sound, with clear abuse paths evaluated. AI-specific threats are addressed, including prompt injection, tool misuse, and data leakage. No blocking issues found.

## Findings

- **info** — `src/cli.ts:800` — The runAdopt function now enforces allowed_roots configuration, matching the MCP tool's behavior. This improves consistency between CLI and MCP surfaces.
  - required: No change required. This is a security improvement.
- **info** — `src/tools/review_execute.ts:100` — Endpoint resolution now falls back to config.defaults.review.endpoint when omitted, improving usability while maintaining validation.
  - required: No change required. This is a security improvement.
- **info** — `src/tools/review_execute.ts:154` — Provider-specific options are now passed through to the LLM client, enabling better control over model parameters like num_ctx and num_predict for Ollama endpoints.
  - required: No change required. This is a security improvement.
- **info** — `src/tools/ship_release.ts:246` — Ship release stdout/stderr is now redacted before being stored in audit logs, addressing a previous security finding.
  - required: No change required. This is a security improvement.
- **info** — `src/tools/ship_release.ts:275` — Project state transitions to 'shipped' only on successful release, preventing incorrect state updates on failure.
  - required: No change required. This is a security improvement.

## Carry-forward

### architecture

_none_

### verification

- (stage 1, info) Assumptions requiring later verification: comments assert CLI/MCP parity for allowed-roots blast radius (`src/cli.ts:803`); comments assert non-Ollama providers tolerate extra `options` objects (`src/util/llmClient.ts:42`); comments assert redaction is sufficient before audit/envelope persistence (`src/tools/ship_release.ts:243`). These are scope-visible and should be tested in later security stages rather than accepted on comment alone.

### security

- (stage 1, info) High-value assets in scope: `<project>/.vcf/project.db`, `~/.vcf/vcf.db`, `~/.vcf/config.yaml`, review prompts/results sent through `review_execute`, and `ship_release` stdout/stderr tails persisted to envelope/audit after redaction. Later stages should test confidentiality/integrity expectations around these stores and flows.
- (stage 1, info) Major trust boundaries identified: operator-supplied filesystem path -> `runAdopt` project writes (`src/cli.ts:787`); config file -> allowed-roots enforcement and endpoint/default selection (`src/cli.ts:807`, `src/config/schema.ts:287`); review payload -> external/local model provider (`src/tools/review_execute.ts:97`, `src/util/llmClient.ts:88`); subprocess `gh` output -> envelope/audit storage (`src/tools/ship_release.ts:243`).
- (stage 1, info) Actor/privilege tiers: local operator invoking CLI; MCP caller invoking server tools; configured endpoint providers with trust levels; GitHub CLI subprocess acting as external tool boundary; local/global SQLite stores as persistence layers. Later stages should verify that trust_level gating and confirm-token controls are consistently applied to privileged actions.
- (stage 2, info) The project now supports adopting existing projects and setting per-tool defaults. These features improve usability but require careful validation of input paths and endpoint configurations.

### compliance

- (stage 1, info) Obligation signals: review payloads may contain project diffs, carry-forward notes, and potentially sensitive repo context sent to configured providers; README now encourages provider-specific options and defaults (`README.md:197`). Later stages should verify whether provider selection/retention/residency expectations are documented strongly enough for projects reviewing regulated or credential-heavy codebases.
- (stage 2, info) Audit redaction for ship_release output is now applied consistently, ensuring sensitive information is not exposed in logs or audit trails.

### supportability

- (stage 2, info) The project now includes a rollback path for the project_adoption_flag migration, allowing users to recover from upgrade issues.

### release_confidence

- (stage 1, info) Security scope clarity is adequate to continue. No Stage-1 blocker found in this diff; proceed to attacker/boundary testing with emphasis on provider data handling, audit redaction fidelity, and boundary enforcement around adopted project paths.

