---
type: review-report
review_type: security
stage: 8
verdict: NEEDS_WORK
run_id: security-8-20260422T004423883Z
created_at: 2026-04-22T00:44:40.352Z
---

# security — Stage 8 — NEEDS_WORK

## Summary

Scoped diff shows application/runtime config changes but provides no reviewed deployment artifacts (Dockerfile, compose, k8s, Terraform, CI workflows, service units, or network/IAM definitions). For Stage 8, that creates a confidence gap: the new project adoption path can register arbitrary existing directories and create runtime state under them, while actual deployment isolation, secret delivery, and pipeline trust remain unverified in-scope. No direct high-severity infrastructure misconfiguration is visible in the diff itself, but infrastructure-security confidence is low because the deployment surface evidence required by this stage is absent.

## Findings

- **warning** — `src/cli.ts:787` — The new `adopt` command onboards arbitrary existing directories into VCF tracking and creates `.vcf/project.db` under the supplied path, but the scoped diff includes no deployment/IaC/pipeline controls showing how adopted projects are isolated from each other or from the host runtime. Stage 8 requires evidence that compromise of one tracked project would be meaningfully contained; that evidence is not present for this newly expanded deployment/runtime surface.
  - required: Provide the actual deployment/runtime artifacts for the adopted-project execution model (container/service definitions, filesystem/volume boundaries, network reachability, and any CI/CD path used to run reviews against adopted projects), or document and enforce isolation boundaries in code/config so this stage can verify containment.
- **warning** — `src/server.ts:158` — A new MCP tool (`project_init_existing`) is registered, expanding the operational deployment surface, but the scoped diff contains no corresponding CI/CD workflow, service-account/IAM, or runtime exposure definitions for the server process that now manages adopted projects. Stage 8 cannot verify least-privilege deployment posture, exposed ports/interfaces, or artifact/deploy trust for the broadened server role from the provided evidence.
  - required: Include the deployment and pipeline definitions actually used for the MCP server, or add reviewed documentation/config that pins runtime model, network exposure, and deployment identity scope for this tool addition.
- **warning** — `src/tools/review_execute.ts:97` — `review_execute` now falls back to `config.defaults.review.endpoint` when the caller omits `endpoint`. This changes deploy-time trust behavior by allowing environment-configured endpoint routing, but the diff provides no infrastructure evidence for how `config.yaml` is injected, protected, or separated across environments. Stage 8 requires the runtime secret/config delivery path to be visible enough to review; here the trust path is implicit.
  - required: Document and provide the deployment mechanism for `config.yaml`/defaults injection (image bake vs runtime mount vs environment generation), including access controls and environment separation, so endpoint-routing trust can be assessed.
- **warning** — `src/util/templates.ts:23` — Template resolution now walks upward until it finds `package.json`, which ties runtime behavior to the host filesystem layout. That is not inherently unsafe, but the scoped diff includes no container/host-boundary artifacts showing whether the process filesystem is constrained (read-only rootfs, limited mounts, non-root user) or whether host paths are broadly exposed. Stage 8 cannot verify filesystem boundary safety for this deployment-sensitive path change.
  - required: Provide the runtime/container/service definitions governing filesystem access, or add explicit hardening controls and documentation showing the process cannot traverse or mount sensitive host paths beyond its package root.

## Carry-forward

### architecture

_none_

### verification

- (stage 8, warning) Next stage should require actual deployment artifacts (CI/CD workflows, container/service manifests, IaC, IAM/service-account definitions) before accepting release-confidence claims for adopted-project support and config-driven endpoint routing.

### security

- (stage 8, warning) Stage 8 confidence is limited by missing deployment evidence. New adopted-project and config-default routing features expand runtime trust assumptions, but isolation, network exposure, filesystem boundaries, and secret/config injection paths were not reviewable from the scoped diff.

### compliance

_none_

### supportability

_none_

### release_confidence

- (stage 8, warning) Do not treat this stage as deployment-secure-by-default. No blocker found in code diff alone, but release confidence is reduced because infrastructure posture for the changed runtime surface was not evidenced in scope.

