---
type: review-report
review_type: security
stage: 1
verdict: PASS
run_id: security-1-20260422T014719179Z
created_at: 2026-04-22T01:47:28.368Z
---

# security — Stage 1 — PASS

## Summary

The diff introduces project adoption functionality ('vcf adopt') and per-step model/endpoint defaults, with corresponding config schema updates and security fixes. Key trust boundaries are clarified around project directory access and MCP/CLI parity. No critical or high-severity issues found in the scope, trust boundaries, or obligations.

## Findings

- **info** — `src/cli.ts:802` — The `runAdopt` function now enforces `allowed_roots` from config, matching the MCP tool's behavior. This improves CLI-MCP parity for path validation.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/review_execute.ts:45` — The `endpoint` field in `review_execute` is now optional, falling back to `config.defaults.review.endpoint` when omitted. This enables default endpoint configuration without per-call overrides.
  - required: No change required; this is a valid enhancement.
- **info** — `src/tools/review_execute.ts:154` — Model ID resolution now falls back to `config.defaults.review.model` when not provided in the tool call. This enables default model selection.
  - required: No change required; this is a valid enhancement.
- **info** — `src/tools/review_execute.ts:162` — Provider-specific options (`providerOptions`) are now passed through to LLM requests, enabling fine-grained control for endpoints like Ollama.
  - required: No change required; this is a valid enhancement.
- **info** — `src/tools/ship_release.ts:246` — Release stdout/stderr is now redacted before being included in audit logs, addressing a previous security finding.
  - required: No change required; this is a security improvement.
- **info** — `src/tools/ship_release.ts:274` — On successful release, project state transitions from 'shipping' to 'shipped', and this change is mirrored in the global registry cache.
  - required: No change required; this is a valid enhancement.
- **info** — `src/util/llmClient.ts:42` — The `providerOptions` field in `ChatCompletionRequest` allows passing endpoint-specific parameters to LLM providers.
  - required: No change required; this is a valid enhancement.
- **info** — `src/util/templates.ts:34` — The `templatesDir()` function now uses a walk-up algorithm to find the package root, fixing a production path resolution issue.
  - required: No change required; this is a security fix.
- **info** — `src/db/schema.ts:199` — Project DB migration v2 adds an `adopted` column to track projects brought in via `project_init_existing`.
  - required: No change required; this is a valid enhancement.

## Carry-forward

### architecture

_none_

### verification

_none_

### security

- (stage 1, info) The project now supports adopting existing directories into VCF tracking, which introduces new trust boundaries around directory access and project state management.

### compliance

- (stage 1, info) The project's handling of release output (stdout/stderr) now includes redaction, which helps with compliance around sensitive data exposure in logs.

### supportability

- (stage 1, info) The project now supports per-step defaults for model and endpoint selection, which improves configurability and reduces per-call argument overhead.

### release_confidence

_none_

