---
type: review-report
review_type: code
stage: 2
verdict: NEEDS_WORK
run_id: code-2-20260422T074201866Z
created_at: 2026-04-22T07:42:51.390Z
---

# code — Stage 2 — NEEDS_WORK

## Summary

System shape is mostly coherent: new inward-loop features are wired across CLI, MCP, DB, review, and docs in a recognizable way, with clear component seams (config/schema, DB migrations, project-scope tools, review overlay resolution, lifecycle reporting). Main flows also look real rather than architecture-shaped. However, Stage-2 cannot pass yet because the project-adoption flow is still implemented twice with separate control paths in the CLI and MCP tool, and the prior Stage-2 duplication finding was not actually closed by a shared core or an accepted-risk carry-forward. That is a real boundary/coherence problem, not a style nit.

## Findings

- **warning** — `src/cli.ts:787` — The adoption workflow is still duplicated across the CLI (`runAdopt`) and the MCP tool (`src/tools/project_init_existing.ts:1`). Both paths separately perform allowed-roots handling, state/name/defaulting, project.db creation/update, and global-registry upsert. For Stage 2 this means the boundary is cosmetic rather than enforced: 'project adoption' is not a single subsystem with one contract, it is parallel implementations that can drift independently. The prior Stage-2 duplication concern is therefore not actually closed in code.
  - required: Extract a shared adoption core used by both surfaces, or explicitly carry this forward as accepted risk with rationale. A PASS here requires either verified consolidation or an explicit accepted-risk close-out; the current diff still leaves two live implementations.

## Carry-forward

### architecture

- (stage 1, info) Stage-1 identity is coherent: this is a developer CLI/MCP tool, not a service. New inward-loop features are materially implemented across CLI, MCP, DB, and review layers (`src/cli.ts:787`, `src/server.ts:161`, `src/tools/lifecycle_report.ts:1`, `src/review/overlays.ts:1`).
- (stage 2, warning) Major components are identifiable and mostly coherent (config/schema, DB, project tools, review pipeline, lifecycle reporting), but the project-adoption capability remains split across `src/cli.ts:787` and `src/tools/project_init_existing.ts:1` instead of enforced through one shared boundary.

### verification

- (stage 1, info) Observed verification surface is substantial for the claimed additions: integration coverage for adoption, lessons, lifecycle report, overlay resolution, response-log migration, plus a perf fixture for 10k-row lifecycle-report assembly (`test/integration/project-init-existing.test.ts:1`, `test/integration/lesson_log.test.ts:1`, `test/integration/lifecycle_report.test.ts:1`, `test/integration/reviewer_overlay_resolution.test.ts:1`, `test/integration/response_log_migration.test.ts:1`, `test/perf/lifecycle_report_10k.test.ts:1`).
- (stage 2, info) Verification surface is substantial for the new inward-loop features, including integration coverage for adoption, lessons, lifecycle report, overlay resolution, response-log migration, and lifecycle-report perf (`test/integration/project-init-existing.test.ts:1`, `test/integration/lesson_log.test.ts:1`, `test/integration/lifecycle_report.test.ts:1`, `test/integration/reviewer_overlay_resolution.test.ts:1`, `test/integration/response_log_migration.test.ts:1`, `test/perf/lifecycle_report_10k.test.ts:1`).

### security

- (stage 1, info) The diff explicitly introduces and documents two sensitive trust boundaries that later stages should inspect closely: cross-project global lesson search and narrative lifecycle-report outbound prompt routing (`CHANGELOG.md:134`, `CHANGELOG.md:136`, `README.md:183`, `README.md:214`).
- (stage 2, info) Architecture exposes two trust-boundary surfaces that later security review should treat as real: cross-project lesson reads via the global lessons DB and outbound narrative lifecycle-report routing through configured endpoints (`src/tools/lesson_search.ts:1`, `src/tools/lifecycle_report.ts:1`, `README.md:183`, `README.md:214`).

### compliance

- (stage 1, info) Project-derived data can now be mirrored to a workstation-global lessons DB and serialized outbound in lifecycle-report narrative mode; docs correctly state that redaction is not confidentiality, so downstream compliance/data review should validate those operator-facing boundaries (`CHANGELOG.md:101`, `CHANGELOG.md:136`, `README.md:183`).
- (stage 2, info) Docs and code align that project-derived lesson/report data can be mirrored or sent outbound depending on config, with redaction but no confidentiality guarantee (`README.md:183`, `README.md:214`, `src/tools/lesson_log_add.ts:1`, `src/tools/lifecycle_report.ts:1`).

### supportability

- (stage 1, info) Ownership and maintenance look real enough for continued review: changelog, specs, manifest/plan/todo, CLI help text, and migration notes all indicate an actively maintained codebase rather than a prompt artifact (`CHANGELOG.md:7`, `plans/phase-2-inward-loop-manifest.md:1`, `plans/phase-2-inward-loop-plan.md:1`, `plans/phase-2-inward-loop-todo.md:1`).
- (stage 2, warning) Change-safety risk remains around adoption because fixes to CLI adoption and MCP adoption must be kept in sync manually while both implementations exist (`src/cli.ts:787`, `src/tools/project_init_existing.ts:1`).

### release_confidence

- (stage 1, info) Stage-1 maturity classification is best read as Internal Tool / MVP, not mature production: the repo is reviewable and feature claims are evidence-backed, but the scope is still an evolving developer-toolchain with active phase plans and upcoming release work (`specs/2026-04-22-phase-2-inward-loop.md:1`, `plans/phase-2-inward-loop-plan.md:1`, `CHANGELOG.md:7`).
- (stage 2, warning) Release confidence is reduced for Stage-2 close-out because a previously acknowledged architecture-duplication issue remains present in the current diff without a verified consolidation or accepted-risk carry-forward.

