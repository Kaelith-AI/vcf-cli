---
type: review-report
review_type: code
stage: 2
verdict: NEEDS_WORK
run_id: code-2-20260422T073320533Z
created_at: 2026-04-22T07:34:50.878Z
---

# code — Stage 2 — NEEDS_WORK

## Summary

The added Phase-2 surfaces mostly form a coherent system: lessons, response-log formalization, overlay resolution, lifecycle reporting, and adoption are identifiable subsystems with real wiring through src/server.ts and the CLI wrappers. Main architectural concern is duplicated adoption logic across the CLI and MCP tool, which creates two parallel implementations of the same boundary-sensitive flow. There is also a docs/implementation mismatch in the release notes: the CHANGELOG claims the MCP surface only grew from 31 to 32 tools, but this diff also registers lesson_log_add, lesson_search, and lifecycle_report.

## Findings

- **warning** — `src/cli.ts:787` — The existing-project adoption flow is now implemented twice: once in the CLI at runAdopt() (src/cli.ts:787-901) and again in the MCP tool at registerProjectInitExisting() (src/tools/project_init_existing.ts:1-193). Both paths perform root validation, project.db creation/update, adopted flagging, and global-registry upsert, but with separate control flow and slightly different edge-case behavior. Stage-2 explicitly calls out duplicated parallel implementations as an architecture-drift risk; this concern is boundary-sensitive because adoption defines how projects enter the system.
  - required: Extract a shared adoption core used by both the CLI and MCP tool so allowed-roots checks, state/name preservation, registry-healing behavior, and future strict/reconstruct modes are enforced from one place.
- **warning** — `CHANGELOG.md:158` — The release notes claim the MCP surface changed from "31 → 32 MCP tools" with only project_init_existing added, but the implementation also registers lesson_log_add, lesson_search, and lifecycle_report in project scope (src/server.ts:173-176) plus project_init_existing in global scope (src/server.ts:161). This is a documentation-vs-implementation mismatch on the system surface area, which undermines the architecture story for reviewers and operators.
  - required: Correct the CHANGELOG tool-count/surface statement to match the actual registered tools in this diff.

## Carry-forward

### architecture

- (stage 1, info) Review surface now spans two artifact classes: runtime code in this repo plus reviewer KB overlays resolved from `config.kb.root` (`src/review/overlays.ts:1-165`, `src/tools/review_execute.ts:140-165`). Later stages should treat KB-pack availability/versioning as part of the effective architecture.
- (stage 2, warning) Subsystem boundaries are mostly clear (overlay resolver, lifecycle report core, lessons DB/mirror, response-log bridge), but project adoption is implemented in parallel in src/cli.ts and src/tools/project_init_existing.ts, creating a real drift vector on a boundary-defining flow.

### verification

- (stage 1, info) Observed scope is backed by new integration/perf coverage for lesson logging, lifecycle reporting, overlay resolution, response-log migration, install-skills common pack, and ship-release state transitions (`test/integration/*.test.ts`, `test/perf/lifecycle_report_10k.test.ts:1-260`). Later stages should validate whether those tests cover edge contracts rather than just happy-path wiring.
- (stage 2, info) The diff adds substantial integration/perf coverage around overlay resolution, response-log migration, lesson logging/search, lifecycle reporting, ship_release transition, and install-skills common-pack wiring; later stages should focus on contract edges rather than basic connectivity.

### security

- (stage 1, info) Two sensitive data-flow surfaces were added/expanded: narrative lifecycle reporting sends project-derived content to a configured endpoint, and review execution can route implicitly via `config.defaults.review` (`README.md:186-221`, `src/tools/lifecycle_report.ts:563-695`, `src/tools/review_execute.ts:98-189`). Later security review should verify redaction and routing assumptions, not just schema correctness.
- (stage 2, info) Narrative lifecycle reporting and review execution both route project-derived content to configured endpoints, with new provider_options/default-routing behavior in play. Security review should verify trust-level assumptions and outbound redaction on those real paths.

### compliance

- (stage 1, info) Lesson and response logging persist human-authored text into project/global SQLite stores (`src/tools/lesson_log_add.ts:61-132`, `src/tools/response_log.ts:78-110`, `src/db/globalLessons.ts:1-145`). No compliance blocker is evident at intake, but later stages should inspect retention and cross-project mirroring behavior.
- (stage 2, info) Lesson data now persists both per-project and in a cross-project mirror DB. Later review should confirm retention and cross-project visibility expectations remain documented and acceptable.

### supportability

- (stage 1, info) Ownership/accountability appears real but lightweight: Kaelith Labs branding is present, and the project includes operational helper scripts plus adoption flows for existing repos (`CHANGELOG.md:1-3`, `src/cli.ts:776-982`, `scripts/dogfood-plan/*.mjs`). Later stages should check whether support expectations stay aligned with 'developer CLI/tool' artifact class.
- (stage 2, info) The new dogfood/stress harnesses improve reviewability and operational introspection, but supportability would improve further if the adopt flow were centralized instead of maintained in two codepaths.

### release_confidence

- (stage 1, info) This diff presents a coherent Phase-2 bundle and is reviewable, but the planning artifacts still describe an in-flight feature release rather than a closed release train (`specs/2026-04-22-phase-2-inward-loop.md:1-92`, `plans/phase-2-inward-loop-plan.md:1-185`). Later release review should verify shipped-vs-planned boundaries explicitly.
- (stage 2, warning) Release notes currently misstate the MCP tool-surface change, so downstream reviewers should treat docs-based architecture claims cautiously until the CHANGELOG is corrected.

