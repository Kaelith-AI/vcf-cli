---
type: review-report
review_type: code
stage: 3
verdict: NEEDS_WORK
run_id: code-3-20260422T002934383Z
created_at: 2026-04-22T00:29:50.760Z
---

# code — Stage 3 — NEEDS_WORK

## Summary

Setup/build signal is only partially strengthened by this diff. The new config/defaults schema and review_execute fallback behavior are covered by focused tests, and the template path fix adds a plausible packaging-oriented resolver. But verification remains materially incomplete for the newly added adoption flow and for the release-state transition: there is no test coverage for the new `adopt` CLI / `project_init_existing` path visible in this diff, and the ship_release tests cover only the failure path while explicitly deferring the success path to a manual real release. That means key user-facing workflow changes can still regress without automated detection. CI/workflow integrity cannot be raised from this diff because no CI changes are present to prove these checks run in automation.

## Findings

- **warning** — `src/cli.ts:790` — The new `adopt` command is a primary setup/onboarding path for existing projects, but no corresponding CLI or integration test is included in the diff. This stage is about whether verification signals would catch meaningful regressions; here they would not catch broken path validation, wrong default state, incorrect registry updates, or re-adoption state preservation bugs in this newly added workflow.
  - required: Add integration coverage for `vcf adopt` / `runAdopt` covering at least: non-existent path rejection, non-directory rejection, default state=`reviewing`, custom valid/invalid state handling, fresh adoption creating `.vcf/project.db`, and re-adoption preserving existing state/name while marking `adopted=1` and updating global registry.
- **warning** — `src/server.ts:160` — `registerProjectInitExisting` is now exposed on the server, but the diff contains no verification for this MCP tool path. The changelog claims a new MCP tool and describes idempotent behavior and state semantics, yet the visible tests only cover config parsing, review defaults, and one negative ship path. This creates a verification gap between claimed capability and demonstrated evidence.
  - required: Add server/integration tests for `project_init_existing` that exercise successful adoption, idempotent re-adoption, allowed-state validation, and persistence of the `adopted` flag in project DB and registry-visible state.
- **warning** — `test/integration/ship_release.test.ts:169` — The new behavior in `ship_release` updates project state to `shipped` on successful release, but the test suite only verifies the negative path and explicitly treats a real 0.4 tag push as the positive-path test. That is not a trustworthy regression guard: automated verification would not catch a broken success transition, stale registry cache update, or accidental unconditional state change.
  - required: Add an automated positive-path test by stubbing the release subprocess/success condition so the suite can assert that `project.state` becomes `shipped` and the global registry cache is updated only when the release succeeds.
- **warning** — `CHANGELOG.md:34` — The changelog itself admits that packaging smoke tests did not call `spec_template`, and while the resolver fix in `templatesDir()` is plausible, the diff adds no automated packaging/build verification proving bundled artifacts can now locate templates. Stage 3 should not treat a code-path explanation as equivalent to a trustworthy verification signal.
  - required: Add a packaging-oriented test or smoke check that exercises template resolution from the built artifact/layout, not only from source execution, so regressions in published-install behavior are caught before release.

## Carry-forward

### architecture

- (stage 3, info) No architectural compromise evident in this diff; changes are localized to CLI/server/config/schema/template resolution and release-state bookkeeping.

### verification

- (stage 3, warning) Verification quality improved for config defaults and review_execute fallback behavior, but important new workflows remain under-tested: no visible automated coverage for adopt/project_init_existing, no positive-path automation for ship_release state transition, and no built-artifact smoke for templates resolution.

### security

- (stage 3, warning) Later security conclusions should be tempered because setup/adoption and release-state changes are not fully covered by automated tests; behavior around state transitions and project registration could regress silently.

### compliance

- (stage 3, info) No compliance-specific issues evident in the scoped diff.

### supportability

- (stage 3, warning) Supportability is reduced by reliance on manual/real-release validation for success-path behavior. Repro steps for failures may be clear, but regression detection for operational flows is incomplete.

### release_confidence

- (stage 3, warning) Release confidence should remain lowered. The changelog acknowledges prior verification blind spots, and this diff does not fully close them with automation for bundled template resolution, adoption workflow, or successful ship_release transition.

