---
type: review-report
review_type: code
stage: 2
verdict: PASS
run_id: code-2-20260422T011136822Z
created_at: 2026-04-22T01:12:18.248Z
---

# code — Stage 2 — PASS

## Summary

The diff introduces a project adoption flow (`vcf adopt` / `project_init_existing`), a flexible configuration system for LLM endpoint/model defaults, and critical fixes for bundled template resolution and Ollama context windowing. The architecture remains coherent: the CLI and MCP server act as thin entry points that delegate to shared utility and database layers. The new adoption logic correctly mirrors the MCP tool's boundary checks (allowed_roots) and maintains the established pattern of local project databases as the source of truth with a global registry as an advisory cache. The configuration resolution logic (explicit > default > legacy) is consistently applied in the review execution path.

## Carry-forward

### architecture

- (stage 1, info) Observed scope expansion is coherent: the tool now supports adopting existing projects into VCF tracking via both CLI (`src/cli.ts`) and MCP (`src/server.ts`). Later stages should verify parity between the two surfaces beyond the allowed-roots check already addressed in the response log.
- (stage 2, info) The 'adopt' flow introduces a partial provenance state (`adopted=1`). Future architectural extensions (like 'strict' or 'reconstruct' modes mentioned in the CHANGELOG) will need to ensure they don't assume the presence of VCF-scaffolded files (AGENTS.md, etc.) when this flag is set.

### verification

- (stage 1, info) Review-related defaults are implemented in config schema and exercised by integration tests (`test/config/schema.test.ts`, `test/integration/review_execute.test.ts`). Later stages should verify behavioral coverage for `project_init_existing`/`vcf adopt` positive and failure paths, since this Stage 1 pass only establishes that the feature is real and wired in.

### security

- (stage 1, info) `runAdopt` now attempts `allowed_roots` enforcement when config is available and intentionally tolerates missing/unloadable config for pre-init operator use. Later security review should assess whether tolerating config parse failure, not just missing config, is an acceptable trust-boundary decision for this CLI artifact class.
- (stage 2, info) The CLI's `runAdopt` intentionally tolerates config parse failures to allow the first adoption to occur before a config exists. This trust-boundary decision is acceptable for an operator-driven CLI but should be monitored if the CLI is ever wrapped in an automated/untrusted environment.

### compliance

- (stage 1, info) The diff introduces an `adopted` provenance flag in project DB schema (`src/db/schema.ts`), which is useful audit metadata. No direct compliance issue is evidenced here, but later stages may want to confirm how adopted vs scaffolded projects are surfaced in reports.

### supportability

- (stage 1, info) Supportability signals improved: CHANGELOG documents schema migration/rollback and published-install template resolution, and `ship_release` includes registry best-effort handling. Later stages should confirm user-facing docs outside CHANGELOG/seed config are updated where needed.

### release_confidence

- (stage 1, info) Release confidence for the changed scope is reasonable: the diff includes schema validation tests and review execution integration tests, plus ship-state regression coverage. The changelog also candidly records prior packaging blind spots, which should be followed in later verification stages.
- (stage 2, info) The fix for `templatesDir()` resolves a critical production-only bug where bundled artifacts failed to find templates. This highlights a gap in the existing test suite (which runs against source); later stages should verify if other path-relative resolutions are similarly brittle to bundling.

