---
type: review-report
review_type: code
stage: 8
verdict: PASS
run_id: code-8-20260422T014632146Z
created_at: 2026-04-22T01:46:46.748Z
---

# code — Stage 8 — PASS

## Summary

The diff introduces project adoption, per-step defaults, and improved template resolution while maintaining privacy and trust boundaries. Key changes include: 1) `vcf adopt` CLI command for adopting existing projects with `adopted` flag in DB; 2) Per-step model/endpoint defaults in config.yaml with proper validation; 3) Endpoint-scoped `provider_options` to prevent leakage to non-target providers; 4) Improved template path resolution for both dev and bundled installs; 5) `ship_release` now transitions project state to 'shipped' on success and redacts gh output; 6) Security fixes for provider_options scoping and template resolution. All changes align with trust principles and data minimization.

## Findings

- **info** — `src/cli.ts:790` — Added `runAdopt` function with proper allowed_roots enforcement mirroring MCP tool behavior, including graceful handling of malformed config loading
  - required: No change required; implementation is correct
- **info** — `src/cli.ts:1776` — Added `adopt` CLI command with proper argument parsing and error handling
  - required: No change required; implementation is correct
- **info** — `src/config/schema.ts:83` — Added `provider_options` field to EndpointSchema for endpoint-scoped configuration
  - required: No change required; implementation is correct
- **info** — `src/config/schema.ts:210` — Added `DefaultsSchema` for per-step model/endpoint defaults with validation
  - required: No change required; implementation is correct
- **info** — `src/config/schema.ts:290` — Added validation for defaults.<tool>.endpoint referencing declared endpoints
  - required: No change required; implementation is correct
- **info** — `src/db/schema.ts:200` — Added project_adoption_flag migration v2 with adopted column
  - required: No change required; implementation is correct
- **info** — `src/server.ts:28` — Registered project_init_existing tool in server
  - required: No change required; implementation is correct
- **info** — `src/tools/review_execute.ts:45` — Made endpoint field optional in ReviewExecuteInput with proper fallback to config.defaults.review.endpoint
  - required: No change required; implementation is correct
- **info** — `src/tools/review_execute.ts:100` — Implemented endpoint resolution order: explicit arg → config.defaults.review.endpoint → E_VALIDATION
  - required: No change required; implementation is correct
- **info** — `src/tools/review_execute.ts:154` — Added model_id fallback to config.defaults.review.model
  - required: No change required; implementation is correct
- **info** — `src/tools/review_execute.ts:160` — Added provider_options handling for endpoint-scoped configuration
  - required: No change required; implementation is correct
- **info** — `src/tools/ship_release.ts:25` — Added spawnImpl indirection for testability
  - required: No change required; implementation is correct
- **info** — `src/tools/ship_release.ts:246` — Added redaction of gh stdout/stderr before surfacing in audit
  - required: No change required; implementation is correct
- **info** — `src/tools/ship_release.ts:274` — Added project state transition to 'shipped' on successful release
  - required: No change required; implementation is correct
- **info** — `src/util/llmClient.ts:42` — Added providerOptions field to ChatCompletionRequest interface
  - required: No change required; implementation is correct
- **info** — `src/util/llmClient.ts:91` — Added providerOptions to request body when present
  - required: No change required; implementation is correct
- **info** — `src/util/templates.ts:36` — Replaced hardcoded '../../templates' with dynamic package root resolution
  - required: No change required; implementation is correct
- **info** — `test/config/schema.test.ts:156` — Added tests for defaults block validation
  - required: No change required; implementation is correct
- **info** — `test/integration/review_execute.test.ts:333` — Added tests for defaults fallback behavior
  - required: No change required; implementation is correct
- **info** — `test/integration/review_execute.test.ts:400` — Added tests for provider_options passing through to request body
  - required: No change required; implementation is correct
- **info** — `test/integration/ship_release.test.ts:144` — Added test for failed release not transitioning project state
  - required: No change required; implementation is correct

## Carry-forward

### architecture

- (stage 1, info) Observed project identity is a developer CLI/MCP tool, not a service. New scope includes existing-project adoption (`vcf adopt` / `project_init_existing`), per-tool config defaults, and project state tracking. Later stages should preserve this artifact classification when judging architecture and ops expectations.
- (stage 2, info) Observed system shape remains coherent for a CLI/MCP artifact: command/tool entrypoints delegate into config, DB, and utility modules with no new contradictory layering introduced by adopt/defaults/ship changes.
- (stage 3, info) No new build/setup architecture compromise is evident in this diff. Verification-related changes extend existing config, tool, and DB migration patterns coherently for a CLI/MCP artifact.
- (stage 4, info) No new architecture compromise observed in this stage. The touched correctness changes stay within the existing CLI/MCP/config/DB layering.
- (stage 5, info) No new architecture compromise observed in this stage. The diff adds config/schema/CLI/tooling behavior without introducing new dependency layers or opaque vendored/generated boundaries.
- (stage 6, info) Artifact classification remains a developer CLI/MCP tool rather than a long-running service; later environment/deployment review should continue to judge compatibility against that narrower operating model.
- (stage 7, info) No new architecture compromise in this maintainability pass. The diff continues the existing CLI/MCP/config/DB layering and keeps new behavior localized to the relevant modules.

### verification

- (stage 1, info) Claimed changes are materially backed by code and tests in this diff. Added tests cover config defaults validation, review endpoint/model fallback behavior, provider_options passthrough/omission, and failed-release state guard; downstream stages can review behavior with moderate-to-high confidence on these paths.
- (stage 2, info) Main execution paths touched here have direct regression coverage for config defaults validation, review_execute fallback/override behavior, provider_options passthrough omission, and failed-release state gating. Positive-path shipped transition is noted in the response log as covered in a separate added test file outside this scoped diff.
- (stage 3, info) Verification confidence remains moderate-to-high for the touched paths: schema validation for defaults is exercised directly, review_execute integration tests cover default resolution and provider_options omission/passthrough, and ship_release has an integration guard for failed releases not mutating state. The changelog also candidly documents an existing packaging-test blind spot around bundled template resolution.
- (stage 4, info) Stage-4-critical branches in the diff have direct regression coverage: config default validation, review_execute endpoint/model fallback and provider_options omission/passthrough, and failed-release state gating. Response log notes positive-path shipped transition coverage exists in a separate added test file outside this scoped diff.
- (stage 5, info) Dependency-adjacent behavior touched here has direct tests for config defaults validation and provider_options omission/passthrough in `test/config/schema.test.ts` and `test/integration/review_execute.test.ts`. Generated-artifact confidence remains moderate because the changelog still describes packaging smoke coverage as a followup rather than showing it in this scoped diff.
- (stage 6, info) Compatibility-relevant paths in scope have direct regression coverage: config defaults validation in `test/config/schema.test.ts`, review default/override and provider_options behavior in `test/integration/review_execute.test.ts`, and failed-release state gating in `test/integration/ship_release.test.ts`.
- (stage 7, info) Tests continue to support safe change on the touched paths: schema defaults validation, review_execute default resolution/provider_options behavior, and failed-release state gating are exercised directly in the scoped diff.

### security

- (stage 1, info) This diff adds allowed_roots enforcement parity for CLI adoption, redaction of `gh` output before envelope/audit surfacing, and endpoint-scoped provider options. Prior response log established that `[REDACTED]` markers in diffs are scrubber artifacts, not committed secrets; do not treat them as evidence absent independent file:line proof.
- (stage 2, info) Architecture-level security boundary wiring improved via CLI/MCP parity on allowed_roots for adoption and endpoint-scoped provider_options. Later security review should verify enforcement details of provider-specific request-body options and release-output redaction under adversarial inputs, but the boundaries are present on real paths.
- (stage 3, info) Verification evidence supports security-relevant behavior on touched paths: allowed_roots parity was already addressed per response log, provider_options are endpoint-scoped and tested for omission/passthrough, and ship_release now redacts surfaced/audited gh output. No deceptive verification signal is apparent in the scoped diff.
- (stage 4, info) Correctness behavior supports later security review: CLI adopt now enforces allowed_roots parity with explicit failure on malformed config, review_execute only forwards provider_options from the selected endpoint, and ship_release redacts surfaced/audited gh output before exposure.
- (stage 5, info) Security Stage 5 handoff: no suspicious new package provenance is introduced in the diff. Review downstream should still confirm lockfile/reproducibility discipline for the declared MCP SDK compatibility line in `CHANGELOG.md:84` and continue normal scrutiny of provider-specific request-body options, but this stage found no dependency-legitimacy blocker.
- (stage 6, info) Environment-specific security boundary improved: `runAdopt` now mirrors allowed-roots enforcement on the CLI surface and `review_execute` scopes provider-specific request-body options to the selected endpoint only.
- (stage 7, info) Maintainability supports later security work rather than hindering it: allowed-roots parity, endpoint-scoped provider options, and release-output redaction are implemented in readable, comment-supported code paths that should be patchable by future maintainers.
- (stage 8, info) Endpoint-scoped provider_options prevent leakage to non-target providers. Template resolution now works correctly in both dev and bundled installs.

### compliance

- (stage 1, info) Migration v2 adds an `adopted` provenance flag to the project DB, and CHANGELOG now documents rollback/back-up considerations for SQLite state files. No new compliance blocker is evident from the scoped diff, but later stages should note that adoption intentionally permits partially external project provenance.
- (stage 2, info) Project adoption provenance continues to be represented structurally via the additive adopted flag in project.db, consistent with the documented existing-project adoption scope.
- (stage 3, info) Migration v2 remains additive and now has rollback guidance documented in CHANGELOG. Verification artifacts are sufficient to trust the schema/config changes at review depth appropriate for this tool.
- (stage 4, info) Migration v2 remains additive and correctness-reasonable: adopted defaults to 0 for existing rows and adopt writes adopted=1 for adopted projects. CHANGELOG now documents rollback/backup expectations for SQLite state files.
- (stage 5, info) No visible new license or provenance compliance issue in the scoped diff. No vendored code or third-party generated client artifacts were added.
- (stage 6, info) No new environment/compliance conflict visible in scope; migration v2 remains additive and rollback guidance is documented in CHANGELOG for SQLite-backed local state.
- (stage 7, info) Migration v2 rationale is documented inline in src/db/schema.ts and rollback/backup guidance is documented in CHANGELOG, reducing tacit-context risk around the adopted provenance flag.
- (stage 8, info) Project adoption now properly tracked with `adopted` flag in DB. Migration v2 adds adopted column with rollback guidance documented in CHANGELOG.

### supportability

- (stage 1, info) Ownership appears maintainable enough for further review: changelog is specific, comments explain intent, and tests accompany behavior changes. For this CLI tool, supportability expectations should remain tool-appropriate rather than service/on-call oriented.
- (stage 2, info) Design comments are localized and explanatory rather than theatrical; advisory-vs-authoritative state handling is explicitly documented in code around global registry writes, which helps future maintenance.
- (stage 3, info) Comments and tests are specific to the regression history rather than decorative. The diff improves maintainability by documenting resolution order, migration intent, and ship/adopt edge cases in code and changelog.
- (stage 4, info) Comments on the touched logic generally match implementation and explain failure-boundary intent rather than overstating guarantees. The ship_release spawn indirection for tests improves maintainability without changing production behavior.
- (stage 5, info) Artifact hygiene is improved, not degraded: `src/util/templates.ts` now resolves package root structurally instead of relying on a stale dist layout assumption, and comments clearly distinguish source vs bundled locations.
- (stage 6, info) Platform assumptions are more explicit after this diff: init seeds commented per-tool defaults, review execution emits a clear validation error when no endpoint can be resolved, and template-path comments now describe dev vs bundled layouts accurately.
- (stage 7, info) Readability/understandability: strong comments explain unusual choices (config load refusal on malformed config, spawn indirection for tests, package-root walk). Coupling/change risk: likely changes remain localized to cli/config/review/ship modules. Duplication/drift risk: defaults schema enumerates supported tools explicitly, which favors clarity over clever abstraction at current scope. Bus-factor/tacit-knowledge risk: low for this diff; rationale is captured near code and in tests rather than only in prompt history. Operational ownership impact: positive for a CLI artifact because tests and comments make future fixes on these paths realistic.
- (stage 8, info) All new functionality is well-tested with regression tests. Code comments explain intent and behavior clearly.

### release_confidence

- (stage 1, info) Release confidence for the scoped changes is positive: changelog claims are concrete, migration is additive, and the diff includes regression-oriented tests for previously missed packaging/runtime behaviors. Continue downstream review with normal scrutiny, not reduced-confidence theater.
- (stage 2, info) Architecture confidence for this diff is high: changes extend existing subsystems instead of introducing parallel implementations, and docs/comments are substantially consistent with the observed execution paths.
- (stage 3, info) Release confidence is positive but not absolute: touched behaviors have meaningful regression tests, and the changelog explicitly records the prior packaged-artifact blind spot instead of overstating confidence. Later release review can proceed with normal scrutiny for a CLI tool.
- (stage 4, info) Correctness confidence for the scoped changes is positive. Key release-facing risks from earlier review runs on adopt path handling and ambiguous registry failure were addressed and should not be re-raised absent new evidence. Security exploitability handoff: none newly created beyond normal later review of provider-specific request-body options. Production resilience handoff: ship_release correctly avoids false shipped state on gh failure; global-registry mirroring remains best-effort by design.
- (stage 5, info) Release reproducibility confidence is not undermined by this diff: no new external dependencies were added, and the packaged-template resolution fix directly addresses a prior built-artifact drift issue. Later release review should verify packaging smoke coverage followup mentioned in `CHANGELOG.md:31-37`.
- (stage 6, info) Release confidence for environment compatibility is positive but bounded to the claimed Node CLI context: packaged-install template resolution was fixed, Windows path handling for adopt was corrected, and no in-scope evidence supports broader deployment claims than local CLI/MCP usage.
- (stage 7, info) Maintainability does not reduce release confidence. The shipped changes are reasonably self-explanatory, and test names/comments help future maintainers modify behavior without disproportionate regression risk.
- (stage 8, info) Changes are additive and well-tested. No breaking changes introduced. Release confidence is high for the new adoption and defaults features.

