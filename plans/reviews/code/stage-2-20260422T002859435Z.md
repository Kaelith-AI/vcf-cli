---
type: review-report
review_type: code
stage: 2
verdict: NEEDS_WORK
run_id: code-2-20260422T002845436Z
created_at: 2026-04-22T00:28:59.435Z
---

# code — Stage 2 — NEEDS_WORK

## Summary

Observed system shape remains mostly coherent: CLI commands in src/cli.ts orchestrate local/project DB updates and registry writes; MCP server registration in src/server.ts exposes tool implementations; config validation in src/config/schema.ts centralizes runtime wiring rules; tool modules implement step-specific behavior. The new features mostly fit those boundaries: defaults are validated in schema then consumed in review_execute, project adoption is wired through both CLI and MCP registration, and release state sync is added inside ship_release. The main architectural weakness in this diff is duplicated adoption/bootstrap behavior split between CLI-local implementation and a separate MCP tool registration path, with no visible shared service boundary in the diff. That increases drift risk for project initialization semantics. There is also an unverified release-state integration path: state mutation was added to ship_release but only the failure path is tested in this diff, so the architecture now claims project lifecycle and registry coherence on successful release without local evidence. Confidence: Medium.

## Findings

- **warning** — `src/cli.ts:790` — Adoption logic is implemented directly inside the CLI command handler path rather than through a shared project-bootstrap/service boundary. In the same change set, src/server.ts registers a separate project_init_existing MCP tool, which implies two entrypoints for the same architectural concern. From the diff there is no evidence they share one implementation or contract. That makes the 'adopt existing project' boundary weak: CLI and MCP can drift on DB initialization, state defaults, idempotency rules, or registry sync while still looking architecturally equivalent.
  - required: Extract the adoption flow into a shared module/service used by both `vcf adopt` and `project_init_existing`, or document and test that the MCP tool delegates to this exact implementation. The goal is one authoritative adoption path for project DB creation/update and registry registration.
- **warning** — `src/tools/ship_release.ts:243` — Successful release now mutates both project DB state and global registry cache, which is a legitimate lifecycle boundary, but the integration evidence in this diff only covers the negative path. The architecture now depends on ship_release being the canonical place where 'shipping' becomes 'shipped', yet the added tests verify only that failed releases do not transition state. That leaves the claimed main-flow wiring partially unverified and lowers confidence in lifecycle coherence.
  - required: Add an automated positive-path test that exercises a successful release flow (with `gh` mocked/stubbed) and verifies both `project.state='shipped'` in the project DB and mirrored registry state. Without that, the system's lifecycle-state architecture is only half-demonstrated in code.

## Carry-forward

### architecture

- (stage 2, warning) Project adoption appears to have two entrypoints (CLI and MCP) with no shown shared implementation. Later stages should treat adoption semantics as drift-prone until delegation/shared service is confirmed.

### verification

- (stage 2, warning) Release lifecycle coherence is only partially verified in this diff: failure path covered, success path not. Code 4 should scrutinize state-transition correctness around ship_release.

### security

- (stage 2, info) review_execute now resolves endpoint through config.defaults.review.endpoint when omitted. Security Stage 3 should verify trust-level/public-endpoint controls still hold under defaulted endpoint selection, not just explicit args.

### compliance

_none_

### supportability

- (stage 2, warning) Direct orchestration logic in CLI for project adoption increases change-safety risk if MCP tool behavior evolves separately. Maintainability review should look for duplicated bootstrap/init flows.

### release_confidence

- (stage 2, warning) Template path resolution change looks structurally sound, but package-layout assumptions and shipped-state transitions should be treated as medium-confidence until bundled/artifact and successful-release paths are covered by tests.

