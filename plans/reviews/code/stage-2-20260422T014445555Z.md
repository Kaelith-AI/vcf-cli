---
type: review-report
review_type: code
stage: 2
verdict: PASS
run_id: code-2-20260422T014438309Z
created_at: 2026-04-22T01:44:45.555Z
---

# code — Stage 2 — PASS

## Summary

The diff introduces coherent architecture for project adoption and per-step defaults, with clear boundaries between CLI, MCP tools, and database layers. Main flows are properly wired with auth checks, config loading, and state transitions. No major architectural incoherencies found.

## Findings

- **info** — `src/cli.ts:790` — New 'adopt' command properly loads config and applies allowed_roots check for parity with MCP tool
  - required: No change needed - this is correct behavior
- **info** — `src/tools/review_execute.ts:100` — Endpoint resolution now follows explicit arg → defaults → legacy fallback order
  - required: No change needed - this is correct behavior
- **info** — `src/tools/ship_release.ts:246` — Release state transition now properly gated on gh exit code (0 = shipped, non-zero = no change)
  - required: No change needed - this is correct behavior
- **info** — `src/util/templates.ts:34` — templatesDir() now uses walk-up to find package root, fixing production bundle path issue
  - required: No change needed - this is correct behavior

## Carry-forward

### architecture

- (stage 1, info) Observed project identity is a developer CLI/MCP tool, not a service. New scope includes existing-project adoption (`vcf adopt` / `project_init_existing`), per-tool config defaults, and project state tracking. Later stages should preserve this artifact classification when judging architecture and ops expectations.
- (stage 2, info) Project adoption and per-step defaults introduce clear subsystem boundaries between CLI, MCP tools, and database layers. The adoption flow properly separates local project.db from global registry operations with non-fatal error handling.

### verification

- (stage 1, info) Claimed changes are materially backed by code and tests in this diff. Added tests cover config defaults validation, review endpoint/model fallback behavior, provider_options passthrough/omission, and failed-release state guard; downstream stages can review behavior with moderate-to-high confidence on these paths.
- (stage 2, info) Main flows properly integrate auth checks, config loading, and state transitions. Tests cover endpoint/model fallback behavior, provider_options passthrough, and failed-release state guard.

### security

- (stage 1, info) This diff adds allowed_roots enforcement parity for CLI adoption, redaction of `gh` output before envelope/audit surfacing, and endpoint-scoped provider options. Prior response log established that `[REDACTED]` markers in diffs are scrubber artifacts, not committed secrets; do not treat them as evidence absent independent file:line proof.
- (stage 2, info) Auth enforcement is consistent between CLI and MCP tools. The adoption flow now mirrors MCP tool's allowed_roots check. No evidence of secret exposure from redaction markers.

### compliance

- (stage 1, info) Migration v2 adds an `adopted` provenance flag to the project DB, and CHANGELOG now documents rollback/back-up considerations for SQLite state files. No new compliance blocker is evident from the scoped diff, but later stages should note that adoption intentionally permits partially external project provenance.
- (stage 2, info) Migration v2 adds adopted flag to project table with documented rollback considerations. No new compliance blockers introduced.

### supportability

- (stage 1, info) Ownership appears maintainable enough for further review: changelog is specific, comments explain intent, and tests accompany behavior changes. For this CLI tool, supportability expectations should remain tool-appropriate rather than service/on-call oriented.
- (stage 2, info) Changes maintain maintainability with clear separation of concerns. Tests accompany behavior changes and cover new functionality including provider_options and state transitions.

### release_confidence

- (stage 1, info) Release confidence for the scoped changes is positive: changelog claims are concrete, migration is additive, and the diff includes regression-oriented tests for previously missed packaging/runtime behaviors. Continue downstream review with normal scrutiny, not reduced-confidence theater.
- (stage 2, info) Release confidence is positive with concrete changelog claims, additive migration, and regression-oriented tests covering previously missed behaviors.

