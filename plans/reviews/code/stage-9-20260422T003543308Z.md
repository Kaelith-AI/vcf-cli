---
type: review-report
review_type: code
stage: 9
verdict: PASS
run_id: code-9-20260422T003439159Z
created_at: 2026-04-22T00:35:43.308Z
---

# code — Stage 9 — PASS

## Summary

The changes introduce critical stability fixes for bundled installs, a new 'adopt' workflow for existing projects, and improved configuration flexibility for LLM routing. The most significant risk—the `templatesDir()` resolution bug—was correctly identified and remediated with a robust walk-up strategy that handles both development and production layouts. The addition of the `adopted` flag in the database schema provides necessary provenance for future auditing and reconstruction modes. Release confidence is high as the changes are bounded, well-tested via integration tests, and address known regressions from the 0.3.x series.

## Findings

- **info** — `src/util/templates.ts:35` — The `findPackageRoot` function includes a fallback to `resolvePath(start, '..', '..')`. While this is a safety net for exotic environments, it may mask configuration errors in some edge cases. However, given the context of the fix for the bundled layout, this is an acceptable trade-off for stability.
  - required: None; documented as a fallback.

## Carry-forward

### architecture

- (stage 9, info) The introduction of the `adopted` flag in the project schema allows the system to distinguish between VCF-native projects and adopted ones, enabling safer future migrations.

### verification

- (stage 9, info) Integration tests for `review_execute` and `ship_release` state transitions verify that the new configuration defaults and release state logic are correct.

### security

_none_

### compliance

_none_

### supportability

_none_

### release_confidence

- (stage 9, info) The fix for `templatesDir()` resolves a critical ENOENT failure in published packages. This significantly increases confidence for the 0.4 dogfood release.

